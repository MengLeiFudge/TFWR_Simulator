# lb_sunflowers_single

## 榜单目标

- 目标资源：`Items.Power`
- 单机路线
- 当前脚本默认目标数量为 `10000`

## 收益机制说明

- 向日葵的新花瓣数来自独立随机：
  - 反编译源码 `Sunflower#OnRestart()` 明确是 `randomSunflower.Next(7, 16)`
  - 也就是每次新种都独立落在 `7..15`
- 因此以下想法是错误的：
  - “现在低点数很多，所以接下来高点数概率会变高”
  - 当前已有的向日葵，不会影响新种向日葵的点数分布
- 倍率规则同样非常明确：
  - 被 harvest 的那株必须等于当前全场向日葵里的最大花瓣值
  - 且场上向日葵总数至少有 `10`
  - 满足时这次 harvest 才能 `x8`
- 所以向日葵的正确理解不是“固定收某个值”，而是“始终尽量收当前最大值”
- 单向日葵之所以相对好做，是因为：
  - 只有一个无人机
  - 当前全场最大值可以直接由它自己掌握
  - 不存在多无人机通信成本

## 当前基线

- 当前默认入口：`main1()`
- 当前路线：5x5 满图向日葵，重种后立刻记录 petal 值，主循环优先收当前最大花瓣
- 当前仓库未见可靠真实游戏时间；当前基线主要来自文件顶的版本结论
- 证据来源：`lb_sunflowers_single.py` 顶部注释与默认入口

## 通用注意事项下的榜单特化

- 单机向日葵更像“用最少的错误 harvest 换到最多的高倍率”
- 这里的关键不是格子越多越好，而是：
  - 最大花瓣识别是否准确
  - 场上是否维持了足够的成熟支撑
  - 收尾阈值是否合理
- 单机不像多机那样难在通信，它的难点主要只是“当前最大值的切换时机”

## 当前版本结论

- `main1`
  - 当前唯一稳定主线
  - 核心思路是 25 株常驻，并优先收最大花瓣
  - 这类“收当前最大值”的路线方向上是对的，单机问题主要在细节节奏而不是通信

## 失败对照

- 草稿里提过“单主位轮转”，文件顶已经明确认为它没有必要
- 也就是说，把向日葵做成单核心路线，当前默认视为不如满图持有
- “已有低点数会推高新种高点数概率”的思路
  - 这条线同样是错误口径
  - 因为新种花瓣数是独立随机 `7..15`

## 下一步优化方向

- 补真实游戏成绩
- 补探针确认：
  - 5x5 是否已经是单机最优图
  - 最大花瓣格在不同 sweep 中的分布稳定性
- 当前不需要像多机那样先解决通信问题

## 候选策略方向（猜测 / 待验证）

### 方向 1：保留满图，严格按当前最大值分阶段收

- 核心思路：不预设“永远等 15”，而是始终跟着当前全场最大值走
- 主瓶颈：最大值从 `15` 切到 `14`、`13` 的时机如果掌握不好，仍会空等
- 可能更强的原因：单机可以直接掌握这个切换，不需要通信
- 优先探针：
  - 每个花瓣阶段持续多久
  - 切值后单位时间 power 是否提高

### 方向 2：末段再放宽到 max-1，而不是全程只盯绝对最大

- 核心思路：前中段严格收当前最大值，末段为了避免久等再放宽到次大值
- 主瓶颈：终盘如果仍死等最大值，可能会空耗时间
- 可能更强的原因：单机能精确知道何时该从“严格最大”切到“次大也收”
- 优先探针：
  - 终盘阶段等待时间占比
  - 放宽到 `max-1` 后的净收益变化

### 方向 3：保留满图，但降低不必要的测量 / 管理开销

- 核心思路：单机没有通信问题，优化点更多在管理成本，比如什么时候测量、什么时候刷新记录
- 主瓶颈：管理动作虽然便宜，但总量也可能不少
- 可能更强的原因：在方向已经对的前提下，单机更可能靠这些细节抠出最后几秒
- 优先探针：
  - 每轮 `measure()` 次数
  - 降低管理频率后是否会导致明显错误 harvest

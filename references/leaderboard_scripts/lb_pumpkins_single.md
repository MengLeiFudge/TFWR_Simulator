# lb_pumpkins_single

## 榜单目标

- 目标资源：`Items.Pumpkin`
- 单机路线
- 当前脚本默认目标数量为 `10000000`

## 收益机制说明

- 南瓜和胡萝卜一样生长在耕地上，而且种植南瓜会消耗胡萝卜。
- 当一个方形区域内所有南瓜都完全成熟时，它们会合并成一个巨型南瓜。
- 这里最关键的阻塞是 `Dead_Pumpkin`：
  - 南瓜完全成熟后有 `20%` 概率枯死
  - 枯死南瓜不会掉落任何东西
  - 枯死南瓜不会参与合并
  - 枯死南瓜还会阻止周围南瓜合并
  - 但在它上面直接种新南瓜会自动移除它，所以不必先 `harvest()`
- 南瓜单块收益规则是：
  - `1x1 -> 1`
  - `2x2 -> 8`
  - `3x3 -> 27`
  - `4x4 -> 64`
  - `5x5 -> 125`
  - `n>=6` 时变成 `n*n*6`
- 这直接推出：
  - 单块最优尺寸就是 `6x6`
  - 因为从 `6x6` 开始，单位面积收益不再继续上升
- 所以单榜南瓜的正确理解非常固定：
  - 直接设世界大小为 `6`
  - 然后围绕一块 `6x6` 做种收循环

## 当前基线

- 当前默认入口：`lb_pumpkins_single()`
- 当前路线：6x6 满田波次策略，首波保留完整实体检查，后续 clean-wave 直接覆盖种植；维持整块南瓜田一起长成、一起结算。
- 当前版本复跑时间：2026-06-09 请求 `698` 完整 `16` 轮均值 `7:40.295`；后续 `finished=false` 取消摘要不作为成绩。
- 历史可靠真实时间：2026-05-02 `main2` 单点收割 + 尾部 `pending <= 6` 施肥 + `0.425/3桶` 补水，并删除低水位单桶 fallback；request `537` 完整 `16` 轮均值 `7:53.507`，request `538` 复跑完整 `16` 轮均值 `7:51.865`
- 证据来源：`lb_pumpkins_single.py` 顶部注释、默认入口与真实请求记录

## 通用注意事项下的榜单特化

- 南瓜单榜的核心不是 companion，而是整块连通与整波成熟
- 主瓶颈通常在：
  - dead pumpkin 补种
  - 尾部未熟格
  - 最终 harvest 顺序
- 因此应优先减少“整波成熟被打断”的次数
- 但在当前规则下，“块大小是否还要猜”这个问题已经基本结束了：
  - `6x6` 就是单块最优尺寸
  - 这榜真正要优化的是 `6x6` 里的执行节奏，而不是再去试 `4x4` / `5x5`

## 当前版本结论

- `main1`
  - 旧版 6x6 简单双循环
  - 已被 `main2` 替代，当前 `.py` 不保留旧实现
- `main2`
  - 当前推荐入口
  - 更明确地围绕“整田一起长、一起收”来写
  - 方向上已经对了：单机就是 `set_world_size(6)` 后围绕一块 `6x6` 做种收
  - 2026-04-24 真实验证：原版 11 轮均值 `10:04.369`；尾部 `pending <= 3` 时使用肥料后 12 轮均值 `9:51.038`
  - 2026-04-24 真实验证：6x6 合并后改为 `goto(0, 0)` 单次收割，14 轮均值 `8:06.252`
  - 关键证据：每轮仍为 `92` 波左右、`harvest=92`，说明收益没有丢失；节省的是合并后扫完整 6x6 的移动成本
  - 2026-04-24 真实验证：在单点收割版本上把尾部施肥阈值从 `pending <= 3` 放宽到 `pending <= 6`，14 轮均值 `8:03.280`
  - 2026-05-02 真实验证：在单点收割版本上把尾部施肥阈值从 `pending <= 6` 收窄到 `pending <= 4`，首跑 15 轮均值 `8:03.044`，但追加复跑退化，未作为稳定刷新保留
  - 2026-05-02 追加复测：恢复 `pending <= 6` 后 request `480` 完整结束 `finished=true runs=15 average=8:05.398`；当前环境旧基线也偏慢，但因 `pending <= 4` 两次合并仍慢于 `8:03.280`，代码继续保守保留 `pending <= 6`
  - 2026-05-02 真实验证：把补水从 `0.75/3桶` 改成 `0.5/2桶` 后，request `516` 完整 `16` 轮均值 `7:57.329`，request `517` 复跑完整 `16` 轮均值 `7:55.978`；确认刷新并保留
  - 2026-05-02 真实验证：保持 `0.5` 阈值但改成 `3` 桶后，request `525` 完整 `16` 轮均值 `7:55.231`，request `526` 复跑完整 `16` 轮均值 `7:56.045`；两次合并约 `7:55.638`，快于 `0.5/2桶` 的 request `516/517` 合并约 `7:56.654`，确认刷新并保留
  - 2026-05-02 真实验证：在 `0.5/3桶` 基线上删除低水位 `0.25/1桶` fallback 后，request `532` 完整 `16` 轮均值 `7:54.905`，request `533` 复跑完整 `16` 轮均值 `7:53.840`；两次合并约 `7:54.373`，快于带 fallback 的 request `525/526` 合并约 `7:55.638`，确认刷新并保留
  - 2026-05-02 真实验证：无低位 fallback 下把高位阈值从 `0.5` 下探到 `0.45` 后，request `534` 完整 `16` 轮均值 `7:53.715`，request `535` 复跑完整 `16` 轮均值 `7:53.942`；两次合并约 `7:53.829`，快于 `0.5/3桶` 无 fallback 的 request `532/533` 合并约 `7:54.373`，确认刷新并保留
  - 2026-05-02 真实验证：无低位 fallback 下把高位阈值从 `0.45` 下探到 `0.425` 后，request `537` 完整 `16` 轮均值 `7:53.507`，request `538` 复跑完整 `16` 轮均值 `7:51.865`；两次合并约 `7:52.686`，快于 `0.45/3桶` 无 fallback 的 request `534/535` 合并约 `7:53.829`，确认刷新并保留
  - 2026-05-30 当前版本复跑请求 `615`：有效四轮稳定均值 `7:55.175`
  - 2026-06-09 clean-wave 结构请求 `693`：首波完整 `prepare_wave()`，后续用 `prepare_clean_wave()` 直接覆盖种植，`harvest_wave()` 只执行单次 `harvest()`；完整 `16` 轮均值 `7:41.985`，刷新旧基线但未稳定进 `1.2x`。
  - 2026-06-09 去掉 `harvest_wave()` fallback 后请求 `695`：完整 `16` 轮均值 `7:40.530`，证明 clean-wave 后单次 `harvest()` 可成立，继续刷新。
  - 2026-06-09 在 request `695` 结构上把补水阈值从 `0.425` 提到 `0.4375` 后，请求 `698` 完整 `16` 轮均值 `7:40.295`，小幅刷新并保留为当前默认；距离 `1.2x` 线 `7:39.985` 仍慢约 `0.31s`。

## 失败对照

- `main1`
  - 已被新版本替代，更多保留为旧结构参考；实现不再保留在 `.py`
- 旧收割路径下尾部 `pending <= 6` 就使用肥料
  - 2026-04-24 真实验证 12 轮均值 `9:50.936`，与当时 `pending <= 3` 的 `9:51.038` 基本持平；但单点收割后该阈值已重新验证为有效
- 全程对所有未熟南瓜使用肥料
  - 2026-04-24 真实验证 12 轮均值 `9:55.157`，慢于尾部阈值版，说明肥料过早使用会浪费在非真正拖尾格上
- 单点收割版本上继续放宽到 `pending <= 9`
  - 2026-04-24 真实验证 14 轮均值 `8:03.552`
  - 对照 `pending <= 6` 的 `8:03.280` 没有进步
- 单点收割版本上收窄到 `pending <= 5`
  - 2026-05-02 请求 `465`：完整结束 `finished=true runs=15 average=8:05.176`。
  - 改法：在 `wait_main2_wave()` 中把尾部施肥条件从 `len(pending) <= 6` 改成 `len(pending) <= 5`。
  - 结论：慢于当时可靠基线 `8:03.280`；当前单点收割版不能把施肥窗口收得更保守，代码已恢复 `pending <= 6`。
- 单点收割版本上放宽到 `pending <= 7`
  - 2026-05-02 请求 `466`：完整结束 `finished=true runs=15 average=8:04.941`。
  - 改法：在 `wait_main2_wave()` 中把尾部施肥条件从 `len(pending) <= 6` 改成 `len(pending) <= 7`。
  - 结论：慢于当时可靠基线 `8:03.280`。
- 单点收割版本上收窄到 `pending <= 4`
  - 2026-05-02 请求 `477`：完整结束 `finished=true runs=15 average=8:03.044`。
  - 改法：在 `wait_main2_wave()` 中把尾部施肥条件从 `len(pending) <= 6` 改成 `len(pending) <= 4`。
  - 有效轮包括 `8:02.773`、`8:08.279`、`8:03.593`、`8:01.835`、`7:58.199`、`8:03.242`、`8:00.696`、`8:03.867`、`8:12.148`、`8:03.999`、`7:58.099`、`8:08.984`、`8:02.382`、`7:55.267`。
  - 2026-05-02 追加请求 `479`：完整结束 `finished=true runs=15 average=8:05.265`。
  - 两次合并约 `8:04.154`，慢于 `pending <= 6` 的 `8:03.280`。
  - 结论：首跑快均值不稳定，代码已恢复 `pending <= 6`。
- 恢复旧基线 `pending <= 6` 后的当前环境复测
  - 2026-05-02 请求 `480`：完整结束 `finished=true runs=15 average=8:05.398`。
  - 有效轮包括 `8:06.299`、`8:12.031`、`8:04.804`、`7:55.859`、`8:04.452`、`8:10.429`、`8:08.554`、`8:09.374`、`8:00.156`、`8:06.289`、`8:03.124`、`8:03.515`、`8:07.799`、`8:04.531`。
  - 结论：这次复测没有刷新，只说明当前环境旧基线也有波动；由于 `pending <= 4` 的两次合并均值更差，当前实现仍保留 `pending <= 6`，`lb_start.py` 记录仍按历史可靠值 `8:03.280`。
- 最后 `<=2` 个 pending 格连续施肥冲刺
  - 2026-05-02 请求 `504`：完整结束 `finished=true runs=15 average=8:02.669`，首跑快于可靠基线 `8:03.280`。
  - 2026-05-02 追加请求 `505`：完整结束 `finished=true runs=15 average=8:04.703`。
  - 两次合并约 `8:03.686`，慢于可靠基线 `8:03.280`。
  - 改法：当 `len(pending) <= 2` 时连续 `use_item(Items.Fertilizer)`，直到成熟、枯死或肥料耗尽；枯死或空格当场补种。
  - 结论：last-mile 连续施肥首跑有波动收益，但复跑退化；代码已恢复原来的 `pending <= 6` 单次施肥逻辑。
- 最后 `<=2` 个 pending 格单次施肥：
  - 2026-05-02 请求 `515`：完整结束 `finished=true runs=15 average=8:06.819`。
  - 改法：把 `wait_main2_wave()` 中的尾部单次施肥阈值从 `len(pending) <= 6` 收窄到 `<= 2`，不引入连续施肥。
  - 有效轮包括 `8:09.921`、`7:59.294`、`8:05.624`、`8:08.784`、`8:06.757`、`8:08.242`、`8:12.599`、`8:02.343`、`8:09.599`、`8:03.398`、`8:03.299`、`8:10.195`、`8:09.998`、`8:01.757`。
  - 结论：严格 last-mile 单次施肥没有减少波数，仍是 `92` waves，均值明显慢于 `8:03.280`；代码已恢复 `pending <= 6`。
- progress 日志阈值提高到 `1M`
  - 2026-05-02 请求 `510`：完整结束 `finished=true runs=15 average=8:05.063`。
  - 改法：在 `maybe_log_main2()` 中把 `curr_pumpkin < last_log_pumpkin[0] + 500000` 改为 `+ 1000000`。
  - 输出行数从原本每轮约 18 条 progress 降到约 9 条 progress，但均值慢于可靠基线 `8:03.280`。
  - 结论：减少 progress 输出没有刷新；代码已恢复 `500000` 阈值。
- 高位补水改成 `0.5/2桶`
  - 2026-05-02 请求 `516`：完整结束 `finished=true runs=16 average=7:57.329`。
  - 2026-05-02 追加请求 `517`：完整结束 `finished=true runs=16 average=7:55.978`。
  - 改法：`maybe_water_main2_slot()` 从 `get_water() < 0.75` 时 `use_item(Items.Water, 3)` 改为 `get_water() < 0.5` 时 `use_item(Items.Water, 2)`；低水位单桶 fallback 保持不变。
  - request `516` 有效轮包括 `8:00.976`、`7:51.445`、`7:57.299`、`7:52.695`、`7:56.992`、`8:02.226`、`7:55.775`、`7:58.749`、`7:55.038`、`8:05.273`、`7:57.460`、`8:00.234`。
  - request `517` 有效轮包括 `7:51.599`、`7:56.874`、`8:03.124`、`7:55.779`、`7:56.992`、`7:55.937`、`7:55.663`、`7:56.367`、`7:44.296`、`7:51.443`、`7:57.890`、`8:02.734`、`7:56.249`。
  - 结论：两次完整统计均明显快于旧可靠 `8:03.280`，说明三桶补水过量，当前默认保留 `0.5/2桶`，`lb_start.py` 更新为 `7:55.978`。
- 只保留 `0.25/1桶` 补水：
  - 2026-05-02 请求 `518`：完整结束 `finished=true runs=15 average=8:12.365`。
  - 改法：删除 `get_water() < 0.5` 时两桶补水分支，只保留 `get_water() < 0.25` 时单桶补水。
  - 有效轮包括 `8:13.437`、`8:06.791`、`8:19.492`、`8:12.968`、`8:15.299`、`8:08.515`、`8:07.181`、`8:13.476`、`8:12.031`、`8:16.992`、`8:13.163`、`8:16.562`、`8:05.999`。
  - 多数轮从 `92` waves 退化到 `93` waves，说明补水太少拖慢成熟；代码已恢复 `0.5/2桶`。
- 早补 `0.75/2桶`：
  - 2026-05-02 请求 `519`：完整结束 `finished=true runs=15 average=8:04.370`。
  - 改法：保持 `0.75` 的较早补水阈值，但每次只用 `2` 桶；低水位单桶 fallback 不变。
  - 有效轮包括 `7:59.399`、`7:58.748`、`8:02.770`、`8:04.179`、`8:02.460`、`8:02.654`、`8:02.847`、`8:03.671`、`8:08.099`、`8:07.460`、`8:15.399`、`8:03.979`、`8:03.984`。
  - 结论：慢于 `0.5/2桶` 的 `7:55.978`，说明刷新主要来自更晚补水到 `0.5`，而不是单纯把 3 桶改成 2 桶；代码已恢复 `0.5/2桶`。
- 晚补 `0.4/2桶`：
  - 2026-05-02 请求 `520`：完整结束 `finished=true runs=16 average=7:57.678`。
  - 改法：把高位补水阈值从 `get_water() < 0.5` 下探到 `< 0.4`，仍使用 `use_item(Items.Water, 2)`；低水位单桶 fallback 不变。
  - 有效轮包括 `7:47.538`、`7:54.599`、`7:53.863`、`8:01.093`、`8:02.890`、`7:56.899`、`8:03.398`、`7:54.179`、`8:02.031`。
  - 部分轮次退化到 `93` waves，均值慢于当前可靠 `7:55.978`；代码已恢复 `0.5/2桶`。
- 略早补 `0.6/2桶`：
  - 2026-05-02 请求 `521`：完整结束 `finished=true runs=16 average=7:58.442`。
  - 改法：把高位补水阈值从 `get_water() < 0.5` 上探到 `< 0.6`，仍使用 `use_item(Items.Water, 2)`；低水位单桶 fallback 不变。
  - 有效轮包括 `8:04.492`、`7:45.546`、`7:57.851`、`7:59.760`、`8:00.156`、`8:05.699`、`8:01.523`、`7:59.199`、`7:55.499`、`8:00.156`、`7:59.843`、`8:01.679`、`7:55.195`。
  - 虽然轮次保持 `92` waves，但均值慢于当前可靠 `7:55.978`；代码已恢复 `0.5/2桶`。
- 微晚补 `0.45/2桶`：
  - 2026-05-02 请求 `522`：完整结束 `finished=true runs=16 average=7:57.684`。
  - 改法：把高位补水阈值从 `get_water() < 0.5` 下探到 `< 0.45`，仍使用 `use_item(Items.Water, 2)`；低水位单桶 fallback 不变。
  - 有效轮包括 `8:14.251`、`7:57.578`、`7:58.476`、`7:53.945`、`8:04.492`、`7:56.751`、`7:57.421`、`7:53.863`、`7:58.593`、`7:55.273`、`7:56.835`、`7:58.671`、`7:52.343`。
  - 仍出现 `93` waves，均值慢于当前可靠 `7:55.978`；代码已恢复 `0.5/2桶`。
- 微早补 `0.55/2桶`：
  - 2026-05-02 请求 `523`：完整结束 `finished=true runs=16 average=7:57.479`。
  - 改法：把高位补水阈值从 `get_water() < 0.5` 上探到 `< 0.55`，仍使用 `use_item(Items.Water, 2)`；低水位单桶 fallback 不变。
  - 有效轮包括 `7:50.976`、`7:57.299`、`7:55.399`、`7:56.562`、`8:03.199`、`8:02.963`、`8:01.835`、`8:01.699`、`7:59.699`。
  - 均值慢于当前可靠 `7:55.978`；代码已恢复 `0.5/2桶`。
- 同阈值 `0.5/1桶`：
  - 2026-05-02 请求 `524`：完整结束 `finished=true runs=15 average=8:04.039`。
  - 改法：保持 `get_water() < 0.5` 阈值，但每次只 `use_item(Items.Water)`，并删除低水位单桶 fallback。
  - 有效轮包括 `8:18.124`、`8:01.835`、`8:00.195`、`7:57.656`、`8:03.899`、`7:55.781`、`7:58.749`、`8:03.161`、`8:14.765`、`8:00.740`、`8:09.335`。
  - 多轮退化到 `93` waves，均值明显慢于当时可靠 `7:55.978`；不保留该候选。
- 同阈值 `0.5/3桶`：
  - 2026-05-02 请求 `525`：完整结束 `finished=true runs=16 average=7:55.231`。
  - 2026-05-02 追加请求 `526`：完整结束 `finished=true runs=16 average=7:56.045`。
  - 改法：保持 `get_water() < 0.5` 阈值，但高位分支改为 `Water >= 3` 时 `use_item(Items.Water, 3)`；低水位单桶 fallback 不变。
  - request `525` 有效轮包括 `8:02.617`、`7:58.436`、`7:57.799`、`7:52.799`、`7:49.699`、`7:49.179`、`7:56.199`、`8:02.099`、`7:58.671`。
  - request `526` 有效轮包括 `7:53.593`、`7:54.056`、`8:06.796`、`7:57.460`、`7:49.799`、`8:02.031`、`8:00.077`、`7:55.099`、`7:55.299`、`7:54.599`、`7:59.570`、`7:50.585`、`7:51.718`、`7:55.999`。
  - request `526` 单次略慢于旧单次最好 `7:55.978`，但只差 `0.067s`；request `525/526` 合并约 `7:55.638`，快于 `0.5/2桶` 的 request `516/517` 合并约 `7:56.654`，确认保留 `0.5/3桶`，`lb_start.py` 更新为 `7:55.231`。
- 同阈值 `0.5/4桶`：
  - 2026-05-02 请求 `527`：完整结束 `finished=true runs=16 average=7:56.726`。
  - 改法：保持 `get_water() < 0.5` 阈值，但高位分支改为 `Water >= 4` 时 `use_item(Items.Water, 4)`；低水位单桶 fallback 不变。
  - 有效轮包括 `7:57.999`、`8:00.234`、`7:49.256`、`7:57.265`、`7:53.984`、`8:00.736`、`8:03.199`、`7:58.515`、`7:59.062`、`7:56.441`。
  - 均值慢于 `0.5/3桶` 的 request `525/526`；出现一轮 `93` waves，说明继续加桶没有稳定收益，代码已恢复 `0.5/3桶`。
- 晚补 `0.45/3桶`：
  - 2026-05-02 请求 `528`：完整结束 `finished=true runs=16 average=7:55.687`。
  - 改法：保持三桶补水，但把高位阈值从 `get_water() < 0.5` 下探到 `< 0.45`；低水位单桶 fallback 不变。
  - 有效轮包括 `8:02.538`、`7:56.599`、`7:58.861`、`8:00.429`、`8:01.752`、`7:51.835`、`7:54.960`、`7:53.399`、`7:53.085`、`7:53.984`、`7:53.042`、`8:02.187`。
  - 单次慢于 `0.5/3桶` 的 request `525`，也慢于 request `525/526` 合并约 `7:55.638`；且出现一轮 `93` waves，代码已恢复 `0.5/3桶`。
- 早补 `0.55/3桶`：
  - 2026-05-02 请求 `529`：完整结束 `finished=true runs=16 average=7:57.245`。
  - 改法：保持三桶补水，但把高位阈值从 `get_water() < 0.5` 上探到 `< 0.55`；低水位单桶 fallback 不变。
  - 结论：均值明显慢于 `0.5/3桶` 的 request `525/526` 合并约 `7:55.638`，早补三桶没有收益；代码已恢复并保留 `0.5/3桶`。
- 低水位 fallback 下探到 `0.2/1桶`：
  - 2026-05-02 请求 `530`：完整结束 `finished=true runs=16 average=7:56.795`。
  - 改法：保持高位 `0.5/3桶`，但把低水位单桶 fallback 从 `get_water() < 0.25` 下探到 `< 0.2`。
  - 有效轮包括 `7:50.499`、`7:55.859`、`7:43.939`、`7:52.146`、`7:59.452`、`8:01.914`、`8:00.099`、`8:05.039`、`7:57.148`。
  - 其中一轮退化到 `93` waves，均值慢于 `0.5/3桶 + 0.25/1桶 fallback` 的 request `525/526` 合并约 `7:55.638`；代码已恢复 `0.25/1桶` fallback。
- 低水位 fallback 上探到 `0.3/1桶`：
  - 2026-05-02 请求 `531`：完整结束 `finished=true runs=16 average=7:56.483`。
  - 改法：保持高位 `0.5/3桶`，但把低水位单桶 fallback 从 `get_water() < 0.25` 上探到 `< 0.3`。
  - 有效轮包括 `8:00.742`、`7:52.382`、`8:03.437`、`8:03.359`、`7:52.578`、`7:51.757`、`7:57.148`、`7:59.837`。
  - 其中一轮退化到 `93` waves，均值慢于 `0.5/3桶 + 0.25/1桶 fallback` 的 request `525/526` 合并约 `7:55.638`；代码已恢复 `0.25/1桶` fallback。
- 无低位 fallback 下晚补 `0.4/3桶`：
  - 2026-05-02 请求 `536`：完整结束 `finished=true runs=16 average=7:55.242`。
  - 改法：删除低水位单桶 fallback 后，把高位三桶补水阈值从 `get_water() < 0.45` 下探到 `< 0.4`。
  - 有效轮包括 `7:54.531`、`7:58.242`、`7:55.468`、`7:55.399`、`8:03.163`、`7:58.085`、`7:50.468`、`7:52.460`、`8:02.599`。
  - 多轮退化到 `93` waves，均值慢于 `0.45/3桶` 无 fallback 的 request `534/535` 合并约 `7:53.829`；不保留该候选。
- 无低位 fallback 下晚补 `0.4125/3桶`：
  - 2026-05-02 请求 `539`：完整结束 `finished=true runs=16 average=7:55.242`。
  - 改法：删除低水位单桶 fallback 后，把高位三桶补水阈值从 `get_water() < 0.425` 下探到 `< 0.4125`。
  - 有效轮包括 `7:55.546`、`7:51.699`、`7:58.864`、`7:55.898`、`7:54.335`、`8:01.484`、`7:51.054`、`7:56.367`。
  - 出现多轮 `93` waves，均值慢于 `0.425/3桶` 无 fallback 的 request `537/538` 合并约 `7:52.686`；不保留该候选。
- 无低位 fallback 下早补 `0.4375/3桶`：
  - 2026-05-02 请求 `540`：完整结束 `finished=true runs=16 average=7:53.292`。
  - 改法：删除低水位单桶 fallback 后，把高位三桶补水阈值从 `get_water() < 0.425` 上探到 `< 0.4375`。
  - 有效轮包括 `7:54.447`、`7:51.367`、`7:53.099`、`7:56.640`、`7:51.199`、`7:50.973`、`7:53.747`、`7:53.945`。
  - 均值慢于 `0.425/3桶` 无 fallback 的 request `538` 单次最好 `7:51.865`，也慢于 request `537/538` 合并约 `7:52.686`；在旧结构下不保留该候选。
- 无低位 fallback 下晚补 `0.421875/3桶`：
  - 2026-05-02 请求 `541`：完整结束 `finished=true runs=16 average=7:54.031`。
  - 改法：删除低水位单桶 fallback 后，把高位三桶补水阈值从 `get_water() < 0.425` 下探到 `< 0.421875`。
  - 有效轮包括 `7:49.446`、`8:01.406`、`7:49.609`、`7:53.199`、`7:48.281`、`7:51.999`、`8:10.499`、`7:50.078`。
  - 长尾波动明显，均值慢于 `0.425/3桶` 无 fallback 的 request `537/538` 合并约 `7:52.686`；不保留该候选。
- 无低位 fallback 下早补 `0.428125/3桶`：
  - 2026-05-02 请求 `542`：完整结束 `finished=true runs=16 average=7:51.690`。
  - 2026-05-02 请求 `543`：完整结束 `finished=true runs=16 average=7:53.707`。
  - 改法：删除低水位单桶 fallback 后，把高位三桶补水阈值从 `get_water() < 0.425` 上探到 `< 0.428125`。
  - 复跑有效轮包括 `7:59.399`、`7:56.679`、`7:52.929`、`7:52.773`、`8:03.398`、`7:51.484`、`7:48.984`、`7:59.374`、`7:48.945`、`7:49.257`、`7:48.593`、`7:51.445`、`7:57.421`。
  - request `542/543` 合并约 `7:52.699`，略慢于 `0.425/3桶` 无 fallback 的 request `537/538` 合并约 `7:52.686`；不保留该候选。
- 无低位 fallback 下早补 `0.4265625/3桶`：
  - 2026-05-02 请求 `544`：完整结束 `finished=true runs=16 average=7:51.997`。
  - 2026-05-02 请求 `545`：完整结束 `finished=true runs=16 average=7:54.180`。
  - 改法：删除低水位单桶 fallback 后，把高位三桶补水阈值从 `get_water() < 0.425` 上探到 `< 0.4265625`。
  - 复跑有效轮包括 `7:53.085`、`7:54.413`、`7:50.399`、`8:05.976`、`7:50.799`、`7:53.899`、`7:46.635`、`7:59.179`、`7:48.124`、`7:59.099`、`7:58.359`、`7:55.624`、`7:42.299`、`8:02.148`、`7:50.156`。
  - request `544/545` 合并约 `7:53.089`，慢于 `0.425/3桶` 无 fallback 的 request `537/538` 合并约 `7:52.686`；不保留该候选。
- 无低位 fallback 下晚补 `0.4234375/3桶`：
  - 2026-05-02 请求 `546`：完整结束 `finished=true runs=16 average=7:54.642`。
  - 改法：删除低水位单桶 fallback 后，把高位三桶补水阈值从 `get_water() < 0.425` 下探到 `< 0.4234375`。
  - 有效轮包括 `7:55.038`、`8:01.054`、`7:55.540`、`8:08.671`、`7:44.414`、`7:48.476`、`7:57.929`、`8:01.093`、`7:57.099`。
  - 均值慢于 `0.425/3桶` 无 fallback 的 request `537/538` 合并约 `7:52.686`；不保留该候选。
- 单点收割版本上收窄到 `pending <= 3`
  - 2026-05-02 请求 `478`：完整结束 `finished=true runs=15 average=8:05.136`。
  - 改法：在 `wait_main2_wave()` 中把尾部施肥条件从 `len(pending) <= 4` 改成 `len(pending) <= 3`。
  - 有效轮包括 `8:02.929`、`8:02.265`、`8:05.546`、`8:10.273`、`8:04.599`、`8:10.468`、`8:04.759`、`8:04.799`、`8:05.581`、`8:04.099`。
  - 结论：慢于 `pending <= 4` 的 `8:03.044`，代码已恢复 `pending <= 4`。
- 合并首轮翻地和首波种植，去掉独立 `init_main2_soil(size)`
  - 2026-04-25 真实验证 14 轮均值 `8:05.190`
  - 慢于当前基线 `8:03.280`
  - 结论：每波种植前增加 `get_ground_type()` / `till()` 检查的长期成本，大于节省首轮独立翻地的收益，不保留实现
- 2026-04-30 只把第一次翻地改成顺手种南瓜：
  - 候选只合并首轮初始化，不改变后续 `prepare_main2_wave()` 的逻辑。
  - 请求 `392` 停表输出 `reached stable leaderboard runs 4 avg=8:03.484`。
  - 尾部有效轮包括 `run=6 time=7:59.335`、`run=7 time=8:08.593`、`run=8 time=8:02.999`、`run=9 time=8:03.007`。
  - 结论：该单变量略慢于当前基线 `8:03.280`，没有刷新；实现已回退，仅保留失败记录。
- 2026-06-09 首轮种植和收割内联：
  - 请求 `690` 把首轮翻地同时种第一波，三轮均值 `7:52.497`，慢于同日基线 `7:50.989`；首轮合并不保留。
  - 请求 `696` 在 clean-wave 基础上内联首轮和收割，完整 `16` 轮均值 `7:41.830`，慢于 request `695` 的 `7:40.530`。
  - 请求 `697` 在内联结构上再把补水阈值调到 `0.4375`，完整 `16` 轮均值 `7:41.216`，仍慢于 request `695`；内联结构不保留。
- “继续试 4x4 / 5x5 是否更优”
  - 这条线当前应视为错误口径
  - 因为收益规则已经把单块最优尺寸钉死在 `6x6`

## 下一步优化方向

- 补 probe，确认：
  - 哪些格子最常成为最后未熟点
  - `Dead_Pumpkin` 对整波节奏的打断程度
- 继续压缩 `prepare_main2_wave` / `wait_main2_wave` 的路径成本，当前真实瓶颈已经从收割扫田转回每波种植、补种和等待扫描
- 尾部施肥阈值当前保留 `pending <= 6`：`<=3`、`<=4`、`<=5`、`<=7` 和 `<=9` 均已验证不稳定或偏慢。
- `pending <= 2` 连续施肥冲刺也已验证不稳定，不能替代当前 `pending <= 6` 单次施肥 fallback。
- `pending <= 2` 单次施肥也已验证偏慢，说明当前不能把肥料窗口收得这么窄。
- 已验证 progress 日志阈值 `1M` 没有刷新，默认保留 `500000` 阈值。
- 已验证 `0.5/2桶` 补水连续两次完整统计刷新，默认保留该补水策略，不再回到 `0.75/3桶`。
- 已验证只保留 `0.25/1桶` 明显偏慢，默认不要低于 `0.5/2桶`。
- 已验证 `0.75/2桶` 偏慢，默认不要把高位阈值提高回 `0.75`。
- 已验证 `0.5/3桶` 快于 `0.5/2桶`；删除低位 fallback 后重新验证，旧结构下 `0.425/3桶` 最快。2026-06-09 clean-wave 结构下 `0.4375/3桶` 请求 `698` 小幅刷新，默认保留；`0.4/3桶`、`0.4125/3桶`、`0.421875/3桶`、`0.4234375/3桶`、`0.4265625/3桶`、`0.428125/3桶` 没有刷新。
- 已验证低水位 fallback 下探到 `0.2/1桶` 或上探到 `0.3/1桶` 都会退化；但完全删除低水位 fallback 在 request `532/533` 连续刷新，默认保留无低位 fallback。
- 2026-05-02 request `547` 临时 probe：
  - 完整结束 `finished=true runs=16 average=7:59.409`，probe 自身有明显输出开销，不作为性能候选。
  - 13 组可见 probe 汇总：`tail_scans=2226`、`dead_events=11065`。
  - tail top：`(0,5)=84`、`(4,4)=78`、`(3,4)=69`、`(3,5)=61`、`(5,5)=58`。
  - dead/replant top：`(1,1)=126`、`(4,4)=103`、`(3,3)=102`、`(0,2)=100`、`(0,1)=95`。
  - 结论：尾部 pending 和死南瓜坐标分散，不适合硬编码单点优先；下一步应测“尾部处理策略”，例如在 `pending <= 6` 时调整遍历顺序或先处理死南瓜/补种点。
- 尾部 pending 最近点顺序：
  - 2026-05-02 请求 `548`：完整结束 `finished=true runs=16 average=7:54.496`。
  - 改法：当 `len(pending) <= 6` 时，按当前位置到最近 pending 的贪心顺序重排，再逐个 `goto()` 处理。
  - 有效轮包括 `7:57.890`、`7:58.499`、`7:55.859`、`8:01.874`、`7:52.109`、`7:47.962`、`7:54.609`、`7:47.265`、`7:55.742`、`7:57.070`、`7:53.710`、`7:53.299`、`7:51.406`、`7:58.981`。
  - 多轮退化到 `93` waves，说明尾部重排会破坏原等待顺序带来的成熟节奏；代码已恢复原 pending 顺序。
- 优化目标应明确写成：
  - 不是继续猜块尺寸
  - 而是把 `6x6` 的种收节奏压到最好

## 候选策略方向（猜测 / 待验证）

### 方向 1：保留 `6x6`，优先处理 `Dead_Pumpkin` 最易出现的坏点

- 核心思路：块尺寸不变，专门优化哪些格子最容易拖成 `Dead_Pumpkin` 阻塞点
- 主瓶颈：只要有一个死点没补上，整块就不能合并
- 可能更强的原因：对单机南瓜来说，修补坏点的优先级比别的局部操作都高
- 优先探针：
  - 哪些位置最常成为最后的死点
  - 提前补这些点后整轮耗时是否下降

### 方向 2：保留 6x6，但把尾部未熟格单独做 last-mile 策略

- 核心思路：大部分格子按原波次推进，最后 1 到 3 个未熟格进入专门处理流程
- 主瓶颈：最后几个格子拖整轮
- 可能更强的原因：尾部专门化后，不必全图陪最后几格等待
- 优先探针：
  - 每轮最后未熟格数量分布
  - last-mile 流程对单轮耗时的影响

### 方向 3：把肥料严格限定到“最后一个坏点”

- 核心思路：不在中段滥用肥料，只在确定它能结束整轮时使用
- 主瓶颈：肥料如果用早了，可能只是加快中间格，却没解决真正拖尾格
- 可能更强的原因：把肥料集中在最后坏点，能最大化整波收益
- 优先探针：
  - 肥料使用位置与整轮结束时间的相关性
  - “最后一格专用肥料”是否优于当前分散补救

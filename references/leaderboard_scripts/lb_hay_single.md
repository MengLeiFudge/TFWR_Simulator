# lb_hay_single

## 榜单目标

- 目标资源：`Items.Hay`
- 单机路线
- 当前脚本默认目标数量为 `100000000`

## 收益机制说明

- 草是最初始资源，只能种在草地上。
- 这和胡萝卜刚好相反：
  - 胡萝卜要求耕地 / Soil
  - 草要求草地
- 所以如果草的伴生是 `Carrot`，这个伴生直接就是废伴生：
  - 因为目标草格必须保持草地
  - 不可能为了胡萝卜再翻成耕地
- 另一个非常关键的事实是：
  - 草基础成长只要 `0.5s = 200t`
  - 浇水后甚至只要几十 `t`
  - 但一次移动是固定 `200t`
  - 一次收割也是固定 `200t`
- 这会推出草单榜最核心的结论：
  - 应该尽可能减少移动
  - 但又不能见熟就收，因为 `harvest()` 自己也要 `200t`
- 这也就是当前打榜思路“原地赌伴生，伴生正确则移向另一格”的根源。
- 还有一个必须写清楚的机制：
  - 伴生一定不会出现和自己相同的作物
  - 所以草的伴生一定不是草
  - 如果伴生落在另一个目标草收获格，那它就一定是废伴生
- 为什么当前单草主结构选 `5x5`，而不是 `4x4`：
  - companion 最远会从当前格出发走 `3` 格
  - 我们希望 companion 尽量不要落在另一个目标草格
  - `5x5` 比 `4x4` 更能减少“伴生落到另一个目标草格”的冲突概率
  - 你给的直观例子是：
    - 在 `4x4` 中，下面那个目标草格往上 `1` 格会冲突，往下 `3` 格也会冲突
    - 在 `5x5` 中，就只剩往上 `1` 格这一种冲突
  - 这里说“重叠率降低一半”，指的就是“伴生落到另一个目标草格，从而变成废伴生”的概率明显下降

## 当前基线

- 当前默认入口：`main4()`
- 当前可靠记录：
  - `main4` 开局一桶：request `487` 完整统计 `2:51.361`，request `488` 完整统计 `2:52.078`
  - `main4` 旧开局两桶手跑记录：`2:52.217`
  - `main10`：`2:53.018`，慢于当前 `main4`
- 2026-05-30 当前版本复跑：请求 `609` 有效 `11` 轮稳定均值 `2:52.141`；后续 `finished=false` 取消摘要不作为成绩。
- 2026-05-02 真实复测 `main4()` 默认入口：请求 `474` 完整结束 `finished=true runs=42 average=2:52.223`，基本复现手跑记录，并快于 `main10` 的 `2:53.018`；因此默认入口从 `main10()` 切回 `main4()`，`main10()` 实现已从 `.py` 删除，只保留文档结论。
- 2026-05-02 开局单桶补水：请求 `487` 完整结束 `finished=true runs=43 average=2:51.361`，请求 `488` 完整结束 `finished=true runs=42 average=2:52.078`；两次加权合并约 `2:51.715`，刷新当前本地最好。
- 2026-04-25 外部 `Save0/hay_single.py` 候选：通过覆盖既有 `gamesave/lb_hay_single.py` 验证，没有向真实存档新增依赖文件；请求 `218` 因当时轮询粒度过粗实际跑到 18 轮，均值 `2:55.942`，慢于当前 `main10` 的 `2:53.018`，不迁入主线。
- 2026-04-25 重新读取当前 `Save0/hay_single.py` 后复测：请求 `248` 因两轮停表检测触发时已跑到 18 轮，均值 `2:57.820`，仍慢于当前 `main10` 记录 `2:53.018`，继续不迁入。
- 2026-04-25 外部 `Save0/hay_single2.py` 候选：独立覆盖既有 `lb_hay_single.py` 验证，请求 `249` 跑到 19 轮，均值 `5:25.510`，明显慢于当前基线，不迁入。
- 当前推荐切回 `main4`，理由是 request `474` 证明它在完整统计下稳定快于 `main10`；如果后续需要定位 `refresh43/refresh44`，应把统计日志加到当前 `main4`，不要保留慢入口。
- 证据来源：`lb_hay_single.py` 顶部版本结论与文件尾默认入口

## 通用注意事项下的榜单特化

- 这个榜单已经明确说明主瓶颈不是“补水后过早收获”，而是 `refresh43/refresh44` 总数
- 换句话说，真正该优化的是刷新次数、回访节奏和 companion 兑现效率，而不是单纯加更多动作
- 草单榜的底层逻辑不是“怎么更快扫图”，而是“怎么在尽量少移动的前提下赌到有效伴生”
- `Carrot` 伴生直接是废伴生
- 另一个目标草格上的伴生也直接是废伴生
- `5x5` 结构的价值就在于：它在 companion 半径 `3` 的规则下，更有效地压低了这类废伴生概率
- 已证伪方向应严格避开，防止重新走回高动作低收益路线

## 当前版本结论

- `main1` 到 `main3`
  - 3x3 路线，明显落后
- `main4`
  - 5x5 双草轮转基线
  - 目前已知最快记录来自这一代；request `487` 完整统计均值 `2:51.361`
  - 2026-05-30 当前版本复跑请求 `609` 稳定均值 `2:52.141`
  - 5x5 的关键价值不是“图更大”，而是更适合压低废伴生重叠
  - 2026-05-02 已把两个开局目标草位从两次单桶补水改为一次单桶补水；request `487/488` 加权合并约 `2:51.715`，快于旧开局两桶 request `474` 的 `2:52.223`
- `main5`
  - 4x4 双草轮转，已明确放弃
- `main6` 到 `main10`
  - 主要围绕日志化、补水验证、other_grass 放行、双浇水合并等方向推进
  - `main10` 慢于当前 `main4`，实现已从 `.py` 删除，文档只保留失败/对照结论
- `main11`
  - 动态改种位方案，显著退化

## 失败对照

- 4x4 双草轮转
- 补水后二次确认
- 另一块目标草位 + Grass companion
- 动态改种位

这些方向都已经在文件顶注明显偏慢或已证伪。
- `main4` 开局批量双桶补水：
  - 2026-05-02 请求 `475` 完整结束 `finished=true runs=42 average=2:52.745`。
  - 改法：把 `main4()` 两个开局目标草位处的连续两次 `use_item(Items.Water)` 改成一次 `use_item(Items.Water, 2)`。
  - 尾部有效轮包括 `2:56.093`、`2:52.999`、`2:52.252`、`2:57.695`、`2:56.499`、`2:53.398`、`2:58.984`、`2:50.078`、`2:52.441`、`2:52.226`。
  - 结论：慢于 `main4` 原版 request `474` 的 `2:52.223`；批量双桶不保留，后续 request `487/488` 已进一步证明开局单桶更快。
- `main4` 开局单桶补水：
  - 2026-05-02 请求 `487`：完整结束 `finished=true runs=43 average=2:51.361`。
  - 尾部有效轮包括 `2:47.176`、`2:51.054`、`2:49.921`、`2:47.899`、`2:55.499`、`2:49.324`、`2:51.740`、`2:54.648`、`2:54.330`、`2:53.699`、`2:48.599`、`2:48.827`、`2:53.232`、`2:52.373`、`2:48.979`。
  - 2026-05-02 追加请求 `488`：完整结束 `finished=true runs=42 average=2:52.078`。
  - 两次加权合并约 `2:51.715`，快于旧开局两桶 request `474` 的 `2:52.223`。
  - 结论：保留开局单桶补水；当时 `lb_start.py` 的 `Hay_Single` 记录更新为 `2:51.361`，当前记录以后续当前版本复跑为准。
- `main4` 开局零桶补水：
  - 2026-05-02 请求 `489`：完整结束 `finished=true runs=42 average=2:52.544`。
  - 尾部有效轮包括 `2:50.854`、`2:51.221`、`2:54.921`、`2:54.921`、`2:56.031`、`2:51.299`、`2:47.890`、`2:50.390`、`2:53.191`、`2:53.398`、`2:55.585`、`2:55.429`、`2:50.251`、`2:54.256`、`2:51.132`。
  - 结论：慢于开局单桶 request `487/488`；开局不能完全不补水，代码已恢复两个目标草位各一次单桶补水。
- `main4` 横向相邻目标草格：
  - 2026-05-02 请求 `499`：完整结束 `finished=true runs=42 average=2:52.359`。
  - 改法：保持 `5x5`、两目标草格、开局单桶补水、循环单桶补水和 Bush-only 伴生策略不变，只把目标草格从竖向 `(4,3)/(4,4)` 改为横向 `(3,4)/(4,4)`，开局 Bush 空洞对应改为 `(1,1)/(1,2)`，目标间移动改为 `East/West`。
  - 尾部有效轮包括 `2:55.039`、`2:52.382`、`2:56.562`、`2:50.312`、`2:49.413`、`2:49.474`、`2:52.576`、`2:54.564`、`2:52.031`、`2:50.028`、`2:51.442`、`2:49.899`、`2:50.429`、`2:52.734`、`2:57.341`、`2:51.171`、`2:54.687`。
  - 结论：慢于当前最好 request `487` 的 `2:51.361`，也慢于单桶保留版复跑 request `488` 的 `2:52.078`；轴向切换没有净收益，代码已恢复竖向目标草格。
- `main4` 废伴生原因 probe：
  - 2026-05-02 请求 `550`：probe 完整结束 `finished=true runs=42 average=2:55.207`；probe 每轮多输出一行，成绩不作为刷新候选。
  - 可见 33 轮 probe 汇总：`refresh43=43260`、`refresh44=43719`、总刷新 `86979`。
  - 目标草格重叠：`reject43_target=854`、`reject44_target=911`，合计 `1765`，约占总刷新 `2.0%`。
  - 非 Bush 类型：`reject43_non_bush=42406`、`reject44_non_bush=42808`，合计 `85214`，约占总刷新 `98.0%`。
  - 结论：当前主要瓶颈不是另一目标草格重叠；继续移动目标格或填 `(1,1)/(2,1)` 的预期收益很低。下一步应看非 Bush 具体类型分布，再判断是否存在混合 companion 田的空间。
- `main4` 非 Bush 类型分布 probe：
  - 2026-05-02 请求 `551`：probe 完整结束 `finished=true runs=41 average=2:56.449`；probe 每轮多输出一行，成绩不作为刷新候选。
  - 可见 17 轮 type probe 汇总：总刷新 `44460`，其中 `bush_target=938`、`carrot=21743`、`tree=21779`，`grass/sunflower/pumpkin/cactus/other` 全部为 `0`。
  - 比例：Bush 目标格重叠约 `2.1%`，Carrot 约 `48.9%`，Tree 约 `49.0%`。
  - 结论：废伴生不是某个单一非 Bush 类型集中拖累，而是 Carrot / Tree 几乎均分。单纯 Tree-only 已在 `lb_hay` 多机榜失败；单草若要做混合 support，必须先证明坐标与类型存在偏置，否则只是把 Bush 命中替换成 Tree 命中。
- `main4` 静态结构数学筛选：
  - 2026-05-31 复核 companion 半径 `3` 与 5x5 wrap 坐标后，当前 `(4,3)/(4,4)` 双目标需要的有效 Bush support 正好是 21 个格子；脚本开局铺设的 21 个 Bush 全部都在有效支撑集合里。
  - 从初始 `(0,0)` 访问这 21 个支撑格并回到 `(4,3)` 的当前手写路径，已经达到 21 次种植 + 21 次移动的下界；不能靠删除“无用 Bush”或大幅缩短开局路径拿到稳定收益。
  - 当前另一目标草格阻塞概率约 `1/24`，与 request `550` 约 `2%` 观察一致，不是主要瓶颈。
  - 因为类型和坐标独立，Tree-only 或 Bush/Tree 坐标混合不会把单类型成功率从约 `1/3` 提到 `2/3`；在没有类型坐标偏置证据前，不进入实机验证。
- `main4` 目标草格枚举筛选：
  - 2026-06-08 `.codex/tests/hay_single_target_layout_budget.py` 枚举 5x5 世界内所有双目标草格，比较减少另一目标草格阻塞与增加移动成本的取舍。
  - 所有距离 `1` 的相邻目标对在循环模型里等价：有效 support 为 `21` 格、Bush-only 成功率约 `31.9%`、估算 `2:52.141`；当前 `(4,3)/(4,4)` 属于这组最优类。
  - 拉开目标距离虽然可能减少 blocked target，但每次南北 / 东西往返都要多付 `200t` 级移动成本，预算上不形成净收益。
  - 结论：不再实机测试 5x5 内目标草格平替或拉开目标；后续若继续 `lb_hay_single`，必须来自类型偏置证据或动态承接新结构，而不是几何微调。
- `main4` 多锚点 Bush-only 轮转筛选：
  - 2026-06-08 `.codex/tests/hay_single_multi_anchor_budget.py` 枚举 5x5 世界中单锚点、双锚点、三锚点、四锚点的 Bush-only 结构，按当前 `2:52.141` 校准。
  - 单锚点纸面 `2:17.515`、success `33.3%`、cycle distance `0`，但该模型没有计入成熟等待，因此只作为实机前筛。
  - 当前相邻双锚点仍是所有双锚点第一：估算 `2:52.141`、success `31.9%`、cycle distance `2`。
  - 三锚点最好估算 `3:09.121`；四锚点最好 `2x2` 估算 `3:08.548`，都慢于当前双草。
  - 单锚点实机 request `676`：90 秒超时，无有效完成轮；`item_snapshot` 显示 `hay=90624` 从 `real_elapsed=1.0` 到 `89.1` 始终不变，`game_time` 推进到 `162098.134`；最终 `finished=false runs=1 average=2730:38.809` 是取消摘要，不作为成绩。
  - 结论：单锚点纸面收益完全来自模型漏掉成熟等待，真实路线几乎不产 Hay；三 / 四锚点预算也慢于当前双锚点。后续不要按单锚点、三锚点或四锚点 Bush-only 轮转继续实机。
- `main4` 非通信定时 Bush/Tree support 预算：
  - 2026-06-09 `.codex/tests/hay_noncomm_schedule_budget.py` 检查在没有请求感知的情况下，盲目把 5x5 support 格周期轮换为 `Bush / Tree` 是否能提高伴生兑现。
  - 当前 5x5 双草结构有 `21` 个 support 格；nearest-neighbor support 循环移动 `23` 步；改写一轮 support 下界约 `13000t`，Bush/Tree 两类型周期约 `26000t`。
  - 如果 anchor 不等待，support 类型和 companion 请求独立，`Carrot` 仍是废伴生，成功率不会突破静态单类型数量级；如果等待目标类型，平均等待约 `13000t`，是当前 Bush-only 期望 reroll 成本 `854.3t` 的 `15.2x`。
  - 结论：不实机盲定时 support 轮换；单草后续只能考虑请求感知的低成本接力或新机制，不能靠预轮换 Tree/Bush 提高利用率。
- `main4` adaptive no-restore Bush/Tree support 筛选：
  - 2026-06-09 `.codex/tests/hay_adaptive_support_memory_screen.py` 检查 support 格保留最近一次 `Bush / Tree` 类型、不恢复，只有当前请求类型不匹配时由当前收割者往返改写。
  - 当前双草结构几何为 `support_events=46`、`total_positions=48`、平均距离 `2.39`；静态 Bush 成功率 `31.9%`，期望 `1054.3t`。
  - adaptive Bush/Tree 把类型成功率纸面提高到 `63.9%`，但约一半 useful accept 要改写 support，`rewrite_ticks=678.3`，总期望 `1104.9t`。
  - 估算从当前约 `2:52.141` 退到 `3:00.404`；不进入实机。
  - 结论：只靠 support 类型记忆 / 不恢复不能提高单草成绩；没有请求格就地 writer 时，不要让当前收割者往返改写 Bush/Tree support。
- `main4` 物理接力 / mailbox helper 预算：
  - 2026-06-09 `.codex/tests/hay_physical_relay_budget.py` 用和多机 hay 相同的实体格编码 / helper 固定等待模型做诊断筛选。
  - 单机榜没有可用并行 helper 槽位；即便按诊断模型给出最乐观的 16 helper 口径，最小 mailbox `signal=1 dist=1` 也从当前约 `2:52.141` 退到 `3:51.400`。
  - 盲扫 support 不等待时退到 `5:44.282`；等待半周期退到 `13:34.116`。
  - 结论：不按物理 mailbox / world-state relay 推进单草；没有通信或零等待完成信号时，实体格信号不可能抵消当前 Bush-only 的低 reroll 成本。
- `main4` 废伴生快速拒绝 / 严格等待预算：
  - 反编译 `Growable.GetCompanion()` 只返回已存的 `companionType` / `companionPos`；`ChooseCompanion()` 在 `Growable.OnRestart()` 中调用。因此重复调用 `get_companion()` 不会重新抽伴生，必须通过 `harvest()` / 重启生长来换下一次 companion。
  - 2026-06-09 `.codex/tests/hay_single_reject_wait_budget.py`：当前 Bush-only 成功率 `31.9%`、期望失败 `2.13` 次，当前 `harvest()` reroll 约 `428.2t`。
  - 反复 `get_companion()` 或只等待不收割会卡在同一个无效 companion；如果等待半个成长窗后再收割，乐观估算退到 `3:36.421`；等待完整成长窗或先浇水再收割退到 `4:20.701`。
  - 结论：不实机“空转等 companion 变化”“重复 get_companion 快速拒绝”或“废伴生先浇水再确认”；这些都不能改变当前 companion，只会推迟必要的 harvest-reroll。
- `main4` 循环未成熟时双桶补水：
  - 2026-05-02 请求 `476` 完整结束 `finished=true runs=42 average=2:52.410`。
  - 改法：保留开局两次单桶补水，只把循环内南北两个 `if not can_harvest(): use_item(Items.Water)` 改成 `use_item(Items.Water, 2)`。
  - 尾部有效轮包括 `2:46.533`、`2:54.453`、`2:56.367`、`2:50.766`、`2:49.303`、`2:50.843`、`2:50.886`、`2:53.638`、`2:50.156`、`2:49.320`、`2:55.289`、`2:53.295`、`2:48.070`、`2:47.799`、`2:55.153`、`2:51.523`、`2:53.544`、`2:54.257`、`2:55.575`、`2:52.656`、`2:53.281`。
  - 结论：略慢于 `main4` 原版 request `474` 的 `2:52.223`；代码已恢复循环单桶补水。
- 再补一条口径级失败对照：
  - `Carrot` 伴生路线默认就是废路线
  - 落在另一目标草格上的伴生也默认就是废伴生

## 下一步优化方向

- 继续围绕“如何减少 `refresh43/refresh44` 总数”推进
- 如需继续定位慢局，直接在当前 `main4` 增加临时统计日志，不再恢复 `main10` 慢入口
- 已验证 `main4` 开局 `use_item(Items.Water, 2)` 没有刷新；当前水量以 request `487/488` 的开局单桶为准。
- 已验证 `main4` 开局单桶补水刷新当前本地最好，默认保留两个目标草位各一次单桶补水。
- 已验证 `main4` 开局零桶补水没有刷新，不再继续往更少开局补水方向试。
- 已验证 `main4` 循环未成熟时 `use_item(Items.Water, 2)` 没有刷新，默认保留单桶补水。
- 已验证 `main4` 横向相邻目标草格没有刷新，默认保留竖向 `(4,3)/(4,4)` 目标草格。
- 已通过 request `550` probe 确认目标草格重叠只占可见刷新约 `2.0%`，不优先继续做目标草格微调或填空洞。
- 已通过 request `551` probe 确认非 Bush 废伴生几乎只由 Carrot / Tree 均分组成；不要直接做 Tree-only 或无证据的混合 support。
- 已通过 2026-05-31 坐标推导确认当前 21 个 Bush support 没有无用铺设；开局支撑路径也已经达到当前支撑集合的移动下界，短期不要继续从删支撑格或改手写路径入手。
- 已通过 2026-06-08 目标草格枚举确认当前相邻目标对属于最优类；不要再做 5x5 目标草位平替或拉开目标间距。
- 已通过 2026-06-08 多锚点筛选和 request `676` 确认单锚点会被成熟等待打穿，三锚点 / 四锚点预算也慢于当前双锚点；不要再做 Bush-only 锚点数量平替。
- 已通过 2026-06-09 非通信定时 Bush/Tree support 预算确认，盲轮换 support 类型无法和随机 companion 请求同步，等待正确类型比当前 reroll 贵一个数量级；不要按预轮换 Tree/Bush 继续实机。
- 已通过 2026-06-09 adaptive no-restore support 筛选确认，当前收割者自己往返改写 Bush/Tree support 仍慢于静态 Bush-only；不要按 support 类型记忆继续实机。
- 已通过 2026-06-09 物理 mailbox / world-state relay 预算确认，实体格信号和固定等待成本远高于当前 Bush-only reroll；单草没有可用并行 helper 槽位，不按这条线继续实机。
- 已通过 2026-06-09 废伴生快速拒绝 / 严格等待预算确认，`get_companion()` 不会重抽 companion；等待或浇水不能改变无效 companion，只会延迟必要的 `harvest()` reroll。
- 重点关注：
  - companion 接受策略是否还能再压 refresh
  - `5x5` 结构是否仍然是 companion 半径 `3` 下最合适的目标草布局
  - 废伴生的具体来源分布

## 候选策略方向（猜测 / 待验证）

### 方向 1：保留 5x5 骨架，但继续压“废伴生接受”（已收窄）

- 核心思路：不动 5x5 主结构，专门优化“哪些伴生应更快识别为废伴生并立即放弃”
- 主瓶颈：当前 refresh 里可能仍有一部分来自废伴生识别不够快
- 可能更强的原因：废伴生越早识别，越少浪费 `200t` 级别的移动和收割
- 优先探针：
  - `Carrot` 废伴生占比
  - “落在另一目标草格”的废伴生占比
  - 更快拒绝后 `refresh43/44` 是否下降
- 当前状态：request `550/551` 已确认废伴生主要是 Carrot / Tree 类型；`get_companion()` 不重抽，所谓“更快拒绝”不能靠重复读取或等待实现，只能是更低成本的 restart / reroll 机制。

### 方向 2：保留 5x5，但改成更严格的“原地赌到有效伴生才移动”（暂缓）

- 核心思路：进一步压缩“无效移动”，把移动几乎完全留给已经确认有效的伴生
- 主瓶颈：草虽然长得快，但每次移动和收割都是硬 `200t`
- 可能更强的原因：越少为了无效伴生而动，越接近草榜的正确成本结构
- 优先探针：
  - 每次有效伴生命中前发生了多少次无效移动
  - 更严格原地等待后总时间是否继续下降
- 当前状态：当前主线已经是 Bush-only 命中后才移动；无效 companion 若不 `harvest()` 重启就不会变化。更严格原地等待会卡住或增加等待成本，不作为实机候选。

### 方向 3：围绕 5x5 继续做“目标草格防重叠”微调

- 核心思路：不换图，而是在 5x5 内微调目标草格位置，继续压低 companion 落到另一目标草格的概率
- 主瓶颈：当前 5x5 虽然比 4x4 好，但未必已经是唯一最优摆位
- 可能更强的原因：如果还能再减少“目标草格之间的废伴生重叠”，就能进一步提高有效 companion 率
- 优先探针：
  - 各目标草格收到废伴生的频率分布
  - 微调目标草位后有效伴生率是否还能上升

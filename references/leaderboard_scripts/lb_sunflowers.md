# lb_sunflowers

## 榜单目标

- 目标资源：`Items.Power`
- 多无人机路线
- 当前脚本按 `100003 power` 作为收尾门槛

## 收益机制说明

- 向日葵的关键不是 companion，而是花瓣数和 harvest 时机。
- 反编译源码 `Sunflower#Harvest()` / `Sunflower#GetYield()` / `Sunflower#OnRestart()` 给出的关键事实是：
  - 每株新种出来的向日葵花瓣数是 `randomSunflower.Next(7, 16)`
  - 也就是独立随机落在 `7..15`
  - 当前已有向日葵的点数，不会影响下一株新向日葵的点数
- 所以以下想法是错误的：
  - “现在低点数很多，接下来新种的大概率会出高点数”
  - 这种带有补偿 / 轮转直觉的想法在这里完全不成立
- harvest 倍率规则是：
  - 被收的那一株必须等于当前全场向日葵里的最大花瓣数
  - 且当前场上向日葵总数至少要有 `10`
  - 满足时这次 harvest 才能拿到 `x8`
- 这意味着多向日葵的核心不是“固定只收 15”，而是：
  - 始终尽量收当前全场最大值
  - 当 `15` 被收得差不多后，当前全场最大值会自然变成 `14`、`13`……
- 所以“只收 15”是明显错误实现：
  - 因为 `15` 的概率本来就低
  - 过不了几轮，场上就很可能已经没有可收的 `15`
  - 但这不代表场上没有可拿 `x8` 的目标，而是当前最大值已经下降了
- 多向日葵真正困难的地方不是收值本身，而是：
  - 不同行无人机如何知道“当前全场最大花瓣值”已经从 `15` 切到 `14`
  - 以及为了同步这个信息要不要付出通信成本

## 当前基线

- 当前默认入口：`main4()`
- 当前 repo 路线：32x32 世界，每行 1 台无人机，成熟就收并重种，有库存阈值才补水
- 这条路线倍率利用率低，但能稳定完成；只收 `15` 的旧 `main2` 已不作为正确基线
- 当前版本复跑时间：2026-05-30 请求 `607` 有效两轮 `3:57.101` / `3:56.523`，稳定均值 `3:56.812`；后续 `finished=false` 取消摘要不作为成绩。
- 历史可靠真实时间：2026-04-24 `main4` 补水版 4 轮均值 `3:54.551`
- 证据来源：
  - `lb_sunflowers.py` 当前实现
  - 反编译里的倍率规则
  - 你这轮对现象的校正

## 通用注意事项下的榜单特化

- 向日葵榜的主收益来自花瓣和高倍率 harvest，而不是 companion
- 这里真正关键的是：
  - 是否始终抓住“当前全场最大花瓣”
  - 是否避免错误 harvest 破坏后续倍率
  - 不同行无人机是否要通信、如何通信、以及不通信时怎么近似同步
- 单向日葵相对容易，因为一个无人机可以直接掌握全场当前最大值
- 多向日葵真正难点在通信：
  - 通信不是不能做
  - 但通信本身也要花时间
  - 有时这部分时间甚至不如直接再种 / 再盲收更划算

## 当前版本结论

- `main1` / `main4`
  - 暴力每熟即收
  - 倍率利用率低，但真实短测能稳定完成
  - 2026-04-24 真实限时验证：`main4` 去掉并发用水后，2 轮均值 `8:24.480`，明显优于卡住的 `main2` 与超慢的安全分阶段版
  - 2026-04-24 继续验证：给 `main4` 恢复“库存足够才补水”后，4 轮均值 `3:54.551`，且不再输出 `Items.Water` 不足警告
  - 2026-04-30 当前默认入口复测：请求 `404` 两轮 `3:55.887` / `3:54.699`，均值 `3:55.293`，基本复确认当前补水版量级，没有刷新
  - 2026-05-30 当前版本复跑请求 `607`：两轮稳定均值 `3:56.812`
- `main2`
  - 旧 repo 默认路线
  - 但“只收 15”这件事本身就是错误实现，不应继续当作主方向
  - 2026-04-24 限时验证：脚本超过 `1min` 未完成，主要输出为 `Items.Water` 不足警告；补充水量检查后仍未在 `1min` 内给出有效均值，当前不继续在这条线上等待
- `main3`
  - 尝试无通信分阶段 `15 -> 7`，逻辑更安全但极慢
  - 2026-04-24 真实限时验证：1 轮 `67:17.599`，不作为默认入口
- `main5`
  - 尝试无通信近似同步阈值循环 `15 -> 7`，不等待全局阶段
  - 2026-04-24 真实限时验证：3 轮均值 `6:19.131`
  - 结论：比 `main4` 补水版 `3:54.551` 明显退化，阈值扫描跳过低花瓣导致空扫成本过高

## 失败对照

- 2026-04-25 外部 `Save0/sunflower.py` 候选：
  - 去掉 `utils` 依赖后覆盖既有 `lb_sunflowers.py` 验证
  - 请求 `258` 30 秒无完成轮，输出大量 `Items.Water` 不足警告
  - `item_snapshot` 显示 Power 持续增长，但缺水警告开销和无完成轮使其不能作为当前候选
  - 复盘修正：不能把这条记录理解为“多机向日葵思路无效”；其核心是 32x32 多无人机高吞吐收割，已经被当前 `main4()` 吸收，并进一步改成每行一机、库存阈值补水，避免旧脚本的缺水告警和入口适配问题
  - 结论：不直接迁入旧脚本，但继续保留并迭代当前 `main4` 补水版
- `main1`
  - 典型失败点是“不看花瓣就收”
  - 这是倍率利用率低的路线，但目前仍比无法同步的分阶段路线更适合作为可完成基线
- “只收 15 花瓣”
  - 这条线同样应视为错误实现
  - 原因不是它太保守，而是它把“当前全场最大值”误写成了“永远等于 15”
- 无通信严格分阶段 `15 -> 7`
  - 真实成绩 `67:17.599`
  - 原因是为避免阶段错位付出了过高扫描与等待成本
- 无通信阈值循环 `15 -> 7`
  - 真实成绩 `6:19.131`
  - 原因是虽然不再等待整阶段，但大量低花瓣成熟株被跳过后仍要反复移动扫描，吞吐不如直接收割重种
- 局部右邻高花瓣优先
  - 2026-04-26 请求 `322`：低于 `10` 花瓣的成熟株先跳过并检查右邻是否更高
  - 运行中没有完成轮，`item_snapshot` 显示 `Power` 约从 `9233` 持续下降到 `1202`
  - 失败原因：即使只做局部偏好，只要跳过当前成熟低花瓣，就会让收割密度快速崩掉；多向日葵当前仍应优先维持高频收割重种
  - 处理：失败实现已从 `.py` 删除，存档入口已同步回稳定 `main4()`
- `main4` 有水就补
  - 2026-04-24 真实验证：4 轮均值 `3:54.527`
  - 对照库存 `>32` 才补水的 `3:54.551` 只有随机级差异，但输出大量 `Items.Water` 不足警告
  - 结论：不作为默认入口，继续保留库存阈值版
- `main4` 高库存 3 桶补水
  - 2026-04-25 真实短窗口 3 轮均值 `3:55.582`
  - 慢于当前 1 桶库存阈值版 `3:54.551`
  - 结论：向日葵主要瓶颈不是单次补水强度，不保留实现
- `main4` 降低补水阈值到 `0.35`
  - 2026-05-02 请求 `453`：runner 输出 `reached stable leaderboard runs 4 avg=3:58.498`。
  - 改法：把 `water_if_available()` 的含水阈值从 `get_water() < 0.425` 降到 `get_water() < 0.35`，库存保护仍为 `num_items(Items.Water) > FIELD_SIZE`。
  - 有效轮为 `3:58.599`、`3:57.337`、`3:58.885`、`3:59.171`。
  - 取消摘要 `finished=false runs=5 average=3:22.378` 不作为刷新成绩。
  - 结论：没有刷新；少补水会拉长成熟等待，慢于当前 `3:54.551` 级别基线，代码已恢复 `0.425`。
- `main4` 提高补水阈值到 `0.50`
  - 2026-05-02 请求 `454`：runner 输出 `reached stable leaderboard runs 4 avg=3:56.434`。
  - 改法：把 `water_if_available()` 的含水阈值从 `get_water() < 0.425` 提高到 `get_water() < 0.50`，库存保护仍为 `num_items(Items.Water) > FIELD_SIZE`。
  - 有效轮为 `3:55.541`、`3:56.554`、`3:56.102`、`3:57.538`。
  - 取消摘要 `finished=false runs=5 average=3:18.310` 不作为刷新成绩。
  - 结论：没有刷新；更早补水比 `0.35` 好，但仍慢于当前 `3:54.551` 级别基线，代码已恢复 `0.425`。
- `main4` 补水阈值 `0.40`
  - 2026-05-02 请求 `455`：runner 输出 `reached stable leaderboard runs 4 avg=3:55.349`。
  - 改法：把 `water_if_available()` 的含水阈值从 `get_water() < 0.425` 降到 `get_water() < 0.40`，库存保护仍为 `num_items(Items.Water) > FIELD_SIZE`。
  - 有效轮为 `3:56.575`、`3:55.976`、`3:52.746`、`3:56.099`。
  - 取消摘要 `finished=false runs=5 average=3:22.935` 不作为刷新成绩。
  - 结论：没有刷新；虽然有一个 `3:52.746` 快单轮，但 4 轮稳定均值仍慢于当前 `3:54.551` 级别基线，也略慢于请求 `404` 的默认复测 `3:55.293`，代码已恢复 `0.425`。
- 初始化阶段不补水
  - 2026-05-02 请求 `456`：runner 输出 `reached stable leaderboard runs 4 avg=3:56.273`。
  - 改法：删除初始化铺满每行向日葵时 `plant(Entities.Sunflower)` 后的 `water_if_available()`，只保留收割重种后的补水。
  - 有效轮为 `3:56.453`、`3:56.452`、`3:57.388`、`3:54.799`。
  - 取消摘要 `finished=false runs=5 average=3:21.234` 不作为刷新成绩。
  - 结论：没有刷新；开局补水不是纯浪费动作，删除后成熟等待回吐收益，代码已恢复初始化补水。
- 初始化阶段直接 `till()`
  - 2026-05-02 请求 `457`：runner 输出 `reached stable leaderboard runs 4 avg=3:54.921`。
  - 2026-05-02 追加请求 `458`：runner 输出 `reached stable leaderboard runs 4 avg=3:56.025`。
  - 改法：初始化铺花时把 `if get_ground_type() != Grounds.Soil: till()` 改成直接 `till()`，其余逻辑不变。
  - 请求 `457` 有效轮为 `3:53.949`、`3:55.572`、`3:55.251`、`3:54.910`；请求 `458` 有效轮为 `3:57.512`、`3:53.917`、`3:57.117`、`3:55.555`。
  - 两次取消摘要 `finished=false runs=5 average=3:20.096` / `3:25.673` 均不作为刷新成绩。
  - 结论：没有稳定刷新；虽然请求 `457` 快于最近默认复测 `3:55.293`，但未超过当时可靠基线 `3:54.551`，追加复跑也退化，代码已恢复地面检查。
- 有水即补
  - 2026-05-02 请求 `459`：runner 输出 `reached stable leaderboard runs 4 avg=3:54.413`。
  - 改法：把 `water_if_available()` 的库存保护从 `num_items(Items.Water) > FIELD_SIZE` 放宽到 `num_items(Items.Water) > 0`。
  - 有效轮包括 `3:54.481`、`3:55.319`、`3:53.451`，第 4 轮因输出截断只保留在 runner 均值里。
  - 取消摘要 `finished=false runs=5 average=3:23.144` 不作为刷新成绩。
  - 结论：速度略快于 `3:54.551`，但 `output.txt` 刷出数百行 `Warning: 尝试使用 Items.Water 但数量不足。`；该路线输出污染严重，不能作为稳定主线，代码已恢复库存保护。
- 补水库存保护 `> 16`
  - 2026-05-02 请求 `460`：runner 输出 `reached stable leaderboard runs 4 avg=3:54.918`。
  - 改法：把 `water_if_available()` 的库存保护从 `num_items(Items.Water) > FIELD_SIZE` 降到 `num_items(Items.Water) > 16`。
  - 有效轮为 `3:54.340`、`3:55.924`、`3:55.468`、`3:53.939`。
  - 取消摘要 `finished=false runs=5 average=3:26.393` 不作为刷新成绩。
  - 结论：没有刷新；该阈值没有出现大量缺水告警，但速度仍慢于 `3:54.551` 可靠基线。
- 补水库存保护 `> 8`
  - 2026-05-02 请求 `461`：runner 输出 `reached stable leaderboard runs 4 avg=3:54.742`。
  - 改法：把 `water_if_available()` 的库存保护从 `num_items(Items.Water) > 16` 降到 `num_items(Items.Water) > 8`。
  - 有效轮为 `3:55.135`、`3:54.284`、`3:53.963`、`3:55.584`。
  - 取消摘要 `finished=false runs=5 average=3:19.747` 不作为刷新成绩。
  - 结论：没有刷新；未见 `> 0` 路线的大量缺水告警，但仍慢于 `3:54.551` 可靠基线。
- 补水库存保护 `> 4`
  - 2026-05-02 请求 `462`：runner 输出 `reached stable leaderboard runs 4 avg=3:54.317`。
  - 2026-05-02 追加请求 `463`：runner 输出 `reached stable leaderboard runs 4 avg=3:55.026`。
  - 改法：把 `water_if_available()` 的库存保护从 `num_items(Items.Water) > FIELD_SIZE` 降到 `num_items(Items.Water) > 4`。
  - 请求 `462` 有效轮为 `3:54.124`、`3:55.084`、`3:54.324`、`3:53.736`。
  - 请求 `463` 有效轮为 `3:55.683`、`3:54.153`、`3:55.981`、`3:54.286`。
  - 两次取消摘要 `finished=false runs=5 average=3:22.406` / `3:22.482` 均不作为刷新成绩。
  - 结论：第一次短窗快于 `3:54.551`，但追加复跑退化；两组 8 轮合并约 `3:54.672`，没有稳定刷新，代码已恢复库存保护。
- 补水库存保护 `> 2`
  - 2026-05-02 请求 `464`：runner 输出 `reached stable leaderboard runs 4 avg=3:55.757`。
  - 改法：把 `water_if_available()` 的库存保护从 `num_items(Items.Water) > FIELD_SIZE` 降到 `num_items(Items.Water) > 2`。
  - 有效轮为 `3:55.243`、`3:55.761`、`3:56.125`、`3:55.897`。
  - 取消摘要 `finished=false runs=5 average=3:20.430` 不作为刷新成绩。
  - `output.txt` 出现 `Warning: 尝试使用 Items.Water 但数量不足。`。
  - 结论：没有刷新且重新出现缺水告警；低于 `> 4` 的库存保护已经不值得继续压，代码已恢复库存保护。
- 通信同步分阶段
  - `send` / `receive` / `get_drone_id` 在当前 leaderboard 脚本环境中未导出
  - 真实探针分别报 `receive 从未被定义`、`get_drone_id 从未被定义`
  - 当前不能作为可行路线
- 行内阈值 + 一行空扫兜底
  - 2026-06-09 请求 `703` 验证。
  - 改法：每个 row worker 从 `min_petals=15` 开始，成熟株低于阈值时先跳过；累计跳过一整行后降低阈值，试图在不通信的情况下提高高花瓣收割比例，同时避免严格分阶段的长期空等。
  - 结果：有效完成轮 `4:38.147` / `4:33.145`，两轮均值 `4:35.646`，明显慢于当前盲收主线 `3:56` 量级；第三轮早期主动停止，`finished=false runs=3 average=4:24.248` 不作为成绩。
  - 结论：即使把兜底窗口压到一行，跳过成熟低花瓣仍会损失吞吐；多机向日葵当前不能靠无通信行内阈值解决主收益错位，代码已恢复盲收主线并重新同步到 `gamesave/`。
- 单行本地最大花瓣优先
  - 2026-06-09 请求 `709` 验证。
  - 改法：每个 row worker 在种植时记录本行 32 株花瓣数；成熟时只收本行当前最大花瓣，若连续跳过满一行仍没收获才 fallback 收当前成熟株。该候选不依赖通信，也不使用固定全局阈值。
  - 结果：`180s` request timeout 内没有有效完成轮；停止时 `power=20289`、`game_time=1814.602`，runner 估算剩余 `eta_game_time=2:03:26`。`[lb_sunflowers] finished=false runs=1 average=30:22.578` 是取消摘要，不作为成绩。
  - 结论：单行本地最大值仍会造成大量成熟株空等，Power 增速从当前盲收的 `3:56` 量级退化到小时级；代码已回退并重新同步正式版到 `gamesave/`。后续不要继续做无通信“每行自己等本行最大”的路线，除非候选能证明几乎不降低成熟收割密度。
- 局部即时换序
  - 2026-06-09 请求 `710` 验证。
  - 改法：保留每次循环至少收当前成熟株；只有当前成熟株花瓣低于 `14`、且东侧 `measure(East)` 为 `14/15`、并且东侧实际也已成熟时，先收东侧再回收当前株，试图在不长期跳过成熟株的前提下提高高花瓣命中。
  - 结果：两轮有效成绩 `4:11.670` / `4:11.075`，稳定均值 `4:11.373`，明显慢于当前盲收主线 `3:56` 量级；计数分别为 `check=297 swap=247 miss=50` 与 `check=354 swap=311 miss=43`，说明分支大量触发但没有净收益。
  - 结论：局部相邻高花瓣不等于全场最大花瓣，额外 `measure()`、往返移动和错误 harvest 标记仍会吞掉收益；代码已回退并重新同步正式版到 `gamesave/`。后续不要再做“只看相邻高花瓣”的局部换序微调，除非能同时证明它接近全局最大且不增加路径成本。
- 删除 harvest 后即时目标检查
  - 2026-06-09 请求 `724/725` 验证。
  - 改法：保留当前盲收高吞吐路线；`row_worker()` 内 `harvest()` 后不再立即调用 `num_items(Items.Power)` 判断是否达标，只在 `while num_items(Items.Power) < POWER_GOAL` 循环头退出，目标是减少每次成熟收割后的高频查询成本。
  - 结果：候选请求 `724` 两轮 `3:55.111` / `3:55.305`，稳定均值 `3:55.208`；回退默认版后同环境请求 `725` 两轮 `3:53.191` / `3:57.439`，稳定均值 `3:55.315`。
  - 结论：候选只比同环境默认均值快约 `0.107s`，低于波动，且没有超过历史可靠默认 `3:54.551`；不作为刷新。代码已恢复默认即时目标检查并重新同步到 `gamesave/`。
- 重种后低成本施肥
  - 2026-06-09 请求 `726` 验证。
  - 改法：保留盲收路线，不测花瓣、不跳过成熟株；只在 harvest 后重种并补水后，如果 `num_items(Items.Fertilizer) > FIELD_SIZE * 2`，则使用一次 `Items.Fertilizer`，试图用库存中自然增长的肥料压低成熟等待。
  - 结果：两轮 `3:55.306` / `3:56.884`，稳定均值 `3:56.095`，慢于同环境默认请求 `725` 的 `3:55.315`，也慢于历史可靠默认 `3:54.551`；结束后已恢复默认脚本并重新同步到 `gamesave/`。
  - 诊断：运行快照显示 fertilizer 在中段长期被压在 `63~64`，但 Power 增速没有覆盖额外 `num_items()` / `use_item()` 动作成本；还观察到 `weird_substance` 从 `0` 增长到 `36`，说明该路线还会引入额外副产物状态扰动。
  - 结论：不保留。后续不要在每次重种后做常规施肥；除非能把触发限制到更少的确定等待窗口，否则施肥动作成本和资源状态扰动会回吐成熟加速。
- active-column / 小活跃场筛选
  - 2026-06-09 `.codex/tests/sunflowers_active_columns_screen.py` 检查“不跳过成熟株，只减少每行活跃向日葵列数”的结构：保留 32 个 row worker，但把每行活跃列数从 `32` 降到 `1..24`，试图减少场上向日葵总数、提高当前株就是全场最大值的概率。
  - 反编译 `Sunflower#Harvest()` 说明错收后下一次原本能 boost 的收割也会被压掉；盲收下实际 x8 概率近似为 `raw_correct^2`。当前 `1024` 株时 raw-correct 约 `11.1111%`，blind boost multiplier 约 `1.0864`。
  - 即使每行只保留 `1` 列，场上仍有 `32` 株，raw-correct 也只有 `11.4042%`，理想无成熟等待估算仅 `3:53.558`，相对当前 `3:54.551` 只快约 `1s`，远达不到 `1.2x` 线 `2:51.089`。
  - 总活跃株数降到 `10` 时 raw-correct 可到 `16.4709%`，但这是“吞吐不下降”的不现实上界，估算也只有 `3:34.153`；一旦按活跃株数量计吞吐损失，就退到小时级。
  - 结论：不进入实机。减少活跃列 / 小活跃场不能作为“不跳过成熟株”的结构突破；多机向日葵若继续，仍必须找到低成本全局最大同步或几乎零路径成本的收割顺序调整。
- 短窗口本行高花瓣先收筛选
  - 2026-06-09 `.codex/tests/sunflowers_short_horizon_high_first_screen.py` 检查剩余的低风险形态：不跳过当前成熟株，但如果本行前方极短窗口内有已记录的 `15` 花瓣，先去收高花瓣再回到当前株。筛选假设目标成熟免费、花瓣记录免费、没有行间失步，是乐观上界。
  - 结果：最佳乐观参数为 `window=6 min_delta=1 min_value=15`，raw-correct 从基线约 `11.1111%` 提到 `56.1835%`，但 detour 率高达 `45.1565%`，不计成熟失败时估算 `2:31.095`。
  - 成熟率敏感性：同一最佳参数在目标成熟率 `90%` 且无额外分支成本时仍约 `2:49.199`，勉强过 `1.2x` 线；成熟率降到 `80%` 时退到 `3:09.874`，已经不过线。加入 `20t` 分支开销后，`90%` 也退到 `2:52.160`。
  - 对照真实 request `710` 的相邻高花瓣即时换序：`check=297 swap=247 miss=50` 与 `check=354 swap=311 miss=43`，成熟命中约 `85%` 左右，且真实两轮 `4:11.670` / `4:11.075` 明显慢于盲收主线。
  - 结论：不改 `lb_sunflowers.py`，不实机本行短窗口 high-first。它只有在不现实的 `90%+` 目标成熟率、几乎零实现成本下才有 probe margin；现有实测成熟命中率和额外移动成本都不足。
- 循环头目标检查削减候选
  - 当前默认 `row_worker()` 在 `while num_items(Items.Power) < POWER_GOAL:` 的循环头每格都会读一次全局 Power；这包含大量未成熟空扫格。request `724/725` 只删除了 `harvest()` 后的即时目标检查，实际仍保留循环头每格查询，因此只看到 `0.107s` 级别差异。
  - 新候选方向是反过来：保留 `harvest()` 后的即时目标检查，但把外层改成无限循环，只在成功收割后读 `num_items(Items.Power)` 决定退出。这样不跳过成熟株、不测花瓣、不改变补水/种植顺序，只减少非成熟空扫时的资源查询。
  - 风险：如果某个 row worker 长时间没有成熟株，它会比默认更晚感知其他行已经达标；不过主脚本的主 row worker 也会持续收割并检查，理论额外尾巴只到下一次本行收割。该候选需要真实短跑确认，不能在当前 `game_tick=0` 执行态下替换默认脚本。
  - 2026-06-10 `.codex/tests/sunflowers_power_check_interval_screen.py` 进一步筛“每 K 格检查一次 Power”的折中模型，把少查 `num_items()` 的收益和主行延迟退出尾巴放在同一抽象单位里比较。
  - 命令：`timeout 60s python3 .codex/tests/sunflowers_power_check_interval_screen.py`，快速完成。结果只有在 `query_cost=0.20` 且 `harvest_rate=0.05` 的保守性很差假设下，`interval=4` 有 `net=1.000`；`query_cost=0.20 harvest_rate=0.10` 与 `query_cost=0.10 harvest_rate=0.05` 只到 `net=0`，更常见成熟率下默认 `interval=1` 最稳。
  - 结论：不改 `lb_sunflowers.py`，也不再把“去循环头 Power 查询”列为游戏恢复后的第一短跑候选。该方向最多是小幅实现成本压缩，且退出尾巴容易回吐收益；Sunflowers 后续仍要找真正全局最大同步或几乎零路径成本的收割顺序调整。
- 无通信阶段阈值近似同步筛选
  - 2026-06-10 `.codex/tests/sunflowers_phase_schedule_screen.py` 筛时间窗 / 行访问次数驱动的无通信阶段阈值，例如 `15 -> 14 -> 13 -> 7`、`15 -> 14 -> 7`、固定 `14/13` 阈值，以及带 `fallback_after` 的短兜底。模型保留当前 32 行 worker 和每行 32 格扫线，且偏向阈值路线：跳过的成熟株会保留、同步免费、没有额外 `measure()` / 记录成本。
  - 原始 `TARGET_POWER=100000 RUNS=180` 和缩小到 `RUNS=60` 都超过 `timeout 60s` 且无输出，已按项目约束改为 `TARGET_POWER=10000 RUNS=50 VISITS_PER_RUN_CAP=16000` 后重跑；命令 `timeout 60s python3 .codex/tests/sunflowers_phase_schedule_screen.py` 约 `6.1s` 完成，`python3 -m py_compile .codex/tests/sunflowers_phase_schedule_screen.py` 通过。
  - 结果：成熟概率 `0.50` / `0.65` 下，所有阶段阈值都慢于盲收，最佳分别只有 `0.8745x` / `0.9403x`。只有在成熟概率 `0.80` 的高成熟乐观档，`phase_15_14_13` 到 `1.0919x`，但 `harvest_rate=0.0894`、`skip/h=7.99`，等价于每次收获前平均跳过约 `8` 株成熟株。
  - 结论：不改 `lb_sunflowers.py`，不实机无通信阶段阈值近似同步。它只有在偏乐观高成熟率、免费跳过、免费同步下才有约 `9%` 纸面收益，远低于进入 `1.2x` 所需空间；且高 `skip/h` 与 request `703` 行内阈值、request `709` 单行本地最大失败同类。
- 本地花瓣 / RNG 预测上界筛选
  - 反编译 `Sunflower#OnRestart()` 显示每株新向日葵来自 `sim.randomSunflower.Next(7, 16)`；`Simulation` 构造时 `randomSunflower` 由内部 `random` 派生，leaderboard 下一轮 seed 又由多个内部 RNG 汇总派生，当前脚本 API 没有暴露 seed 或 `randomSunflower` 状态。
  - 即使先忽略 seed 不可见问题，`.codex/tests/sunflowers_local_threshold_upper_bound.py` 也给了一个更强的乐观上界：假设每个 row worker 免费知道当前成熟株花瓣值、免费跳过、不付 `measure()` / 成熟等待 / 记录成本，并且 `32x32` 全场最大按几乎总是 `15` 处理。
  - 命令：`python3 -m py_compile .codex/tests/sunflowers_local_threshold_upper_bound.py` 通过；`timeout 60s python3 .codex/tests/sunflowers_local_threshold_upper_bound.py` 快速完成。
  - 结果：盲收 `ppv=12.2963`；本地阈值 `15` 的上界 `ppv=13.3333`，只有 `1.0843x`；其他阈值 `8..14` 都慢于盲收。
  - 结论：不改 `lb_sunflowers.py`，不再把“本地记录花瓣 / RNG 预测 / 只收本地高值”作为无通信主线。没有全局同步收割顺序时，即使完美本地花瓣信息也到不了 `1.2x`；真实实现还会额外支付跳过、测量和成熟等待成本。
- “已有低点数会让新种高点数概率变高”的思路
  - 这条线也是错误口径
  - 因为新种花瓣数是独立随机的 `7..15`

## 下一步优化方向

- 当前默认先保留 `main4` 补水版，因为它能稳定完成且没有并发用水告警
- 2026-04-30 本轮不改多机代码，只复测当前默认入口：请求 `404` 两轮均值 `3:55.293`，说明当前无通信高吞吐路线仍稳定，但没有继续接近 #1
- 已验证降低补水阈值到 `0.35` 没有刷新，默认保留 `0.425`。
- 已验证提高补水阈值到 `0.50` 没有刷新，默认保留 `0.425`。
- 已验证补水阈值 `0.40` 没有刷新，默认保留 `0.425`。
- 已验证初始化阶段不补水没有刷新，默认保留初始化补水。
- 已验证初始化阶段直接 `till()` 没有稳定刷新，默认保留地面检查。
- 已验证有水即补虽然略快但会大量刷缺水警告，默认保留 `num_items(Items.Water) > FIELD_SIZE` 库存保护。
- 已验证补水库存保护 `> 16`、`> 8`、`> 4` 和 `> 2` 都没有稳定刷新；`> 2` 已重新出现缺水告警，默认保留 `> FIELD_SIZE`。
- 当前多机路线基本没有有效通信；在没有重新量化通信成本、现有 `main4()` 盲收收益、以及近似同步可行性之前，不应继续临时加分阶段或局部偏好逻辑
- 请求 `703` 证明行内阈值 + 短兜底仍然慢于盲收；后续不要继续沿“成熟低花瓣先跳过，等阈值下降”这条线微调兜底长度。
- 请求 `709` 证明“每行自己等本行最大”也会退到小时级；局部最大值优先如果需要跳过成熟株，同样会破坏收割密度。
- 请求 `710` 证明“不长期跳过成熟株”的相邻即时换序仍慢于盲收；局部高花瓣偏好不能替代全局最大判断，且额外测量 / 移动成本会直接回吐收益。
- 请求 `724/725` 证明删除 harvest 后即时目标检查只有 `0.107s` 级别波动差异，不应替换默认退出逻辑。
- 请求 `726` 证明 harvest 后每次重种的常规施肥会慢于默认；肥料不能当作免费成熟加速，除非触发点更少且不会引入额外资源状态扰动。
- `.codex/tests/sunflowers_active_columns_screen.py` 证明减少活跃列 / 小活跃场不会带来结构收益；32 株以上当前最大仍几乎总是 `15`，x8 raw-correct 概率接近 `1/9`，而更小活跃场的吞吐损失会远超倍率收益。
- `.codex/tests/sunflowers_short_horizon_high_first_screen.py` 证明本行短窗口 high-first 只有在 `90%+` 目标成熟率和极低分支成本下才有上界空间；request `710` 的真实相邻命中率约 `85%` 且明显慢于盲收，因此不进入实机。
- `.codex/tests/sunflowers_power_check_interval_screen.py` 证明周期性削减 Power 查询没有足够理论 margin；不再把“去循环头 Power 查询、保留 harvest 后查询”作为恢复后优先短跑候选。
- `.codex/tests/sunflowers_phase_schedule_screen.py` 证明无通信时间窗 / 阶段阈值近似同步只有在高成熟率乐观档才有约 `9%` 纸面收益，并且平均每次收获前跳过约 `8` 株成熟株；不进入实机。
- `.codex/tests/sunflowers_local_threshold_upper_bound.py` 证明完美本地花瓣信息、免费跳过、无实现成本的阈值上界也只有 `1.0843x`；没有全局同步收割顺序时，RNG / 本地测量不能作为压进 `1.2x` 的主结构。
- 下一步若继续优化，应优先找“不依赖 send/receive 的近似同步”：
  - 降低错误 harvest 的同时，不引入 `main3` 那种全阶段扫描成本
  - 用少量花瓣测量 / 局部最大值估计，判断是否能把 `8:24.480` 拉近 #1

## 候选策略方向（猜测 / 待验证）

### 方向 1：按“当前全场最大值”分阶段收，而不是固定只收 15

- 核心思路：主逻辑不是“永远等 15”，而是先收完当前全场最大值，再切到下一个值
- 主瓶颈：多机下最难的是判断“什么时候该从 15 切到 14”
- 可能更强的原因：这才符合真实倍率规则，不会因为场上 15 已空而彻底停机
- 优先探针：
  - 每个花瓣值阶段持续多久
  - 从一个值切到下一个值的最佳时间窗

### 方向 2：无通信近似同步分段收法

- 核心思路：先通过实测得到“一行从大到小收一轮大约要多久”，再把这个时间切成均匀段落
- 核心假设是：
  - 其他行的无人机大致也会在这个时间段内把当前值收完
  - 所以不用通信，也能大概率在正确时间切到下一个花瓣值
- 主瓶颈：真正通信会浪费时间，但完全不通信又容易阶段错位
- 可能更强的原因：这是一种“用时间窗代替通信”的近似同步，可能正好卡在性价比最好的位置
- 优先探针：
  - 单行 `15->14->13...` 的实测耗时
  - 不同行之间这个耗时的方差有多大

### 方向 3：通信版全局最大值同步

- 核心思路：显式维护一个全局当前最大花瓣值，让所有行按同一阶段收
- 主瓶颈：通信和同步本身会吃掉时间
- 可能更强的原因：如果同步成本低于错位导致的错误 harvest / 空转损失，就可能比无通信更稳
- 优先探针：
  - 同步一次全局最大值需要多少额外 tick
  - 通信后 `x8` harvest 占比是否显著提升

### 方向 4：通信太贵时，直接增加盲收比例

- 核心思路：如果通信成本过高，就接受一部分阶段误差，靠更快重种和更高刷新频率去覆盖
- 主瓶颈：严格同步可能本身就比错位损失更贵
- 可能更强的原因：多机场景里，“略带误差但更快”有时会优于“完全正确但太慢”
- 优先探针：
  - 通信版与盲收版的单位时间 power 增长
  - 盲收导致的错误 harvest 是否真的大到不可接受

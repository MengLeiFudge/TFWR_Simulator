# lb_dinosaur

## 榜单目标

- 目标资源：`Items.Bone`
- 当前脚本目标数量为 `33488928`
- 核心不是普通农业收益，而是恐龙帽状态下尾巴长度与结算倍率

## 结算机制说明

- 这题本质上就是经典贪吃蛇，只是落在 TFWR 的农场网格里。
- 关键机制可以直接概括成：
  - 戴上 `Dinosaur_Hat` 后，如果有足够仙人掌，会自动购买一个苹果并放在无人机脚下
  - 当无人机站在苹果上再次移动时，会吃掉苹果，尾巴增长一节
  - 如果还有足够仙人掌，就会尝试在随机空位生成下一颗苹果
  - 戴恐龙帽时不能穿越农场边界
  - 尾巴会填充无人机走过的地块；如果试图移动到尾巴上，`move()` 会失败
  - 但尾巴最后一节会在移动时移开，所以可以踩到“刚刚离开的尾尖”
  - 苹果上调用 `measure()` 会返回下一颗苹果的位置
- 反编译源码 `DinosaurHat#OnMove()` / `DinosaurHat#BoneCount()` 给出的关键事实是：
  - 初始 `move()` 成本是 `400 ticks`
  - 每吃一颗苹果，后续 `move()` 成本会减少 `3%`，向下取整
  - 卸下恐龙帽时，骨头收益是 `尾巴长度^2 * 科技倍率`
- 当前满级科技倍率是 `32`
- 在 `32x32` 农场里一共 `1024` 个格子
- 最终满盘状态下：
  - 头占 1 格
  - 尾巴最多占剩下的 `1023` 格
- 所以当前 leaderboard 目标值的来源非常明确：
  - `33488928 = 1023 * 1023 * 32`
- 这意味着当前恐龙榜不是“滚很多轮尾巴再结算”，而是一次到位问题：
  - 目标就是在一局里把尾巴吃到 `1023`
  - 然后卸帽一次性结算

## 当前基线

- 当前默认入口：`main()`
- 当前路线：外部 `Save0/dinosaur2.py` 适配版，先构造覆盖 32x32 的安全路径表，前期围绕 `measure()` 给出的苹果位置做受限追踪，达到约 `size * size / 3` 尾长后沿固定路径吃到不能继续移动，再 `clear()` 结算
- 当前真实验证成绩：
  - `request_id=214`：7 轮均值 `15:26.925`
  - `request_id=238`：`15:30.624` / `15:17.650`，两轮均值 `15:24.137`
  - `request_id=239`：真源同步后 `15:56.873` / `15:17.538`，两轮均值 `15:37.206`
  - `request_id=619`：2026-05-30 当前版本复跑 `15:26.484` / `15:09.843`，稳定均值 `15:18.164`；后续 `finished=false` 取消摘要不作为成绩
- 对比旧默认：
  - `main6` 固定哈密顿回路 3 轮均值 `33:10.221`
  - `dinosaur2` 约快 `2.15x`
- 适配修复：
  - 直接迁入为 `lb_dinosaur.py`，无外部依赖
  - 同步工具对 `lb_dinosaur` 生成 `lb_start.py` 时使用外部入口验证过的 `1000` 模拟速度

## 通用注意事项下的榜单特化

- 这个榜单的主收益是“单局满尾巴平方结算”，不是高频 harvest
- 因为目标值刚好等于 `1023^2 * 32`，所以不存在“差不多够了就提前收”的空间
- 活得足够久、避免自撞、控制苹果获取路径，比普通移动微优化更重要
- 世界大小、路径覆盖率、是否能安全吃到远处苹果，是第一优先级变量
- 同时这又不是普通等权最短路：
  - 越早吃到苹果，后续 `move()` 越便宜
  - 所以早期苹果的时间价值比后期更高

## 当前版本结论

- `main1..main4`
  - 早期贪心 / 局部打分方案
  - 文件明确给出的结论是：死亡过早，bones 依赖多局累积，太慢
- `main5`
  - 两阶段贪心 + parity 限制路径
  - 已明确落后 #1 约 11 分钟
- `main6`
  - 旧默认固定哈密顿回路
  - 路线：不赌局部最优，转而用完整覆盖路径换绝对安全
  - 在当前目标值下，这条线的正确性基础非常清楚：只要能安全吃满 `1023` 个苹果，就能一次结算达标
  - 2026-04-24 真实验证 3 轮均值 `33:10.221`
- 外部 `Save0/dinosaur2.py`
  - 当前默认路线
  - 2026-04-25 通过覆盖既有 `gamesave/lb_dinosaur.py` 验证，没有向真实存档新增依赖文件
  - 真实请求 `214`：7 轮均值 `15:26.925`
  - 真实请求 `238`：2 轮均值 `15:24.137`
  - 真源同步后请求 `239`：2 轮均值 `15:37.206`
- 2026-05-30 当前版本复跑请求 `619`：2 轮稳定均值 `15:18.164`
- 2026-06-09 当前版本复跑请求 `713`：两轮 `15:19.335` / `15:57.851`，均值 `15:38.593`；第二轮慢但仍证明当前主线可稳定完成，后续 `finished=false` 取消摘要不作为成绩。
  - 明显优于旧 `main6` 的 `33:10.221`，已迁入仓库真源
  - 该路线通过新模组每秒 `item_snapshot` 确认在持续消耗 `Cactus` 并推进运行，不属于状态机假活
  - 为什么更快：
    - 旧 `main6` 等价于全程沿大回路等待随机苹果，安全但平均等待步数高
    - `dinosaur2` 前期持续利用 `measure()` 的苹果坐标，围绕当前尾长和路径表做受限追踪，尽早吃苹果
    - 恐龙移动成本会随吃苹果递减，早期更快吃到苹果会把后续所有移动都一起降成本
    - 达到足够尾长后再切回路径化安全行走，避免纯贪心后期自撞
  - 之前搜索空间的问题：
    - 过度相信“全程哈密顿最稳就是主线”，低估了早期苹果对后续移动成本的复利收益
    - 对 `main5` 失败的解读过宽，把“早期贪心 + 后期保命”整个方向也一起否掉了

## 失败对照

- 2026-04-25 外部 `Save0/dinosaur.py` 候选：
  - 覆盖既有 `lb_dinosaur.py` 验证，请求 `262` 无完成轮，最终 `leaderboard finished without completed runs`
  - `item_snapshot` 显示 Cactus 持续缓慢下降但 Bone 始终为 0
  - 慢于并且不如已迁入的 `Save0/dinosaur2.py` 可结算路线，不迁入
- 早期“见苹果就追”的贪心类方案
  - 核心问题不是找不到苹果，而是活不到足够长
- `main5`
  - 两阶段相比纯贪心更稳，但仍不足以逼近榜前
- “不吃满也许更快”的阈值收割思路
  - 2026-04-24 实测过分段结算 `main7`：先 `tail=900` cash-out，再按剩余骨头计算 `tail=487` 补第二段
  - 真实结果：`main7 cashout tail=900 bones=25920000 time=2072.51`，随后 `tail=487 bones=33509408 time=3656.93`
  - leaderboard 一轮成绩 `60:56.953`
  - 明显差于 `main6` 的 `33:10.221`
  - 结论：虽然骨头可以跨多次卸帽累计，但重置尾巴后重新吃第二段苹果的成本远高于满尾最后阶段成本；该实现已从 `.py` 回退，只保留失败结论
- 前期阈值 64 的直接贪心前缀
  - 2026-04-30 请求 `397` 验证。
  - 改法：在当前 `dinosaur2` 主线前加入 `length < 64` 的直达苹果贪心，优先走能缩短到 `measure()` 苹果坐标的方向，直达方向被挡时退回原 `path[x][y]`。
  - 结果：到 `real_elapsed≈138s`、`game_time≈33466` 仍然 `leaderboard_runs=0`、`bone=0`，`cactus` 长时间停在 `999998848`；被主动停止。
  - 停止摘要 `[lb_dinosaur] finished=false runs=1 average=558:54.759` 是取消摘要，不作为有效完成轮。
  - 结论：无安全结构的直接贪心会卡死在无结算状态；该实现已从 `.py` 回退。后续仍应做“带安全窗口/可回路恢复”的激进贪心，而不是裸直达。
- 前期受限 BFS 短路追苹果
  - 2026-05-02 请求 `558` 验证。
  - 改法：维护脚本侧尾巴占用，前 `length < size * 4` 时用短窗口 BFS 直接追 `measure()` 给出的苹果坐标，找不到短路再退回原路线。
  - 结果：到 `real_elapsed≈108s`、`game_time≈8079` 仍然 `leaderboard_runs=0`、`bone=0`，`cactus` 仅从 `999999872` 降到 `999997760`；手动停止。
  - 停止摘要 `[lb_dinosaur] finished=false runs=1 average=136:26.132` 是取消摘要，不作为有效完成轮。
  - 结论：每步都重算完整 BFS，并且在脚本侧维护尾巴占用，会让计算和保守避让成本过高；后续 BFS 只能做限深 / 限节点 / 限尾长的轻量前缀，且每步必须用 `can_move()` 兜底。
- 前期限额 BFS 前缀
  - 2026-05-02 请求 `559` / `560` / `561` 验证。
  - 改法：在原路线前加入不维护脚本侧尾巴的轻量 BFS 前缀，先后测试 `length < size` 与 `length < size // 2` 两档，BFS 自身限深 / 限节点，实际移动前仍用 `can_move()` 兜底。
  - 结果：
    - `length < size` 可完成，但两轮 `16:35.820` / `16:20.268`，均值 `16:28.044`，慢于当前 `15:24` 左右基线。
    - `length < size // 2` 会在只吃少量苹果后停止推进，`cactus` 长时间停在 `999999488`，无完成轮。
  - 结论：直接插入任意 BFS 路线会破坏当前 `dinosaur2` 的后续安全形状；如果继续做 BFS，不能只是从当前位置到苹果的普通最短路，必须把“回到原安全路径 / 保持蛇形可恢复”作为 BFS 状态的一部分。
- 哈密顿顺序短切
  - 2026-05-02 请求 `563` 验证。
  - 改法：给固定回路建立 `path_index`，只允许 BFS 短切沿哈密顿顺序前进，避免跳到回路后方；窗口限制为 `max_depth=size+size//2`、`max_nodes=size*5`，仅在 `length < size*size/3` 的前期阶段启用。
  - 结果：`15:25.390` / `15:13.593`，两轮均值 `15:19.492`。
  - 结论：这是目前唯一有正收益且可完成的改法，保留为当前候选。
- 放宽短切窗口或延长追踪阶段
  - 2026-05-02 请求 `564` / `565` / `567` / `570` 验证。
  - 结果：
    - 延长到 `length < size*size/2` 且放宽到 `max_depth=size*3`、`max_nodes=size*8`：`16:03.164` / `16:21.796`。
    - 只放宽窗口、不延长追踪阶段：`15:47.538` / `15:35.038`。
    - 提前在 `length < size*8` 切回固定回路：`16:33.984` / `16:51.640`。
    - 延长到 `length < size*size/2` 但保持小窗口：`15:58.281` / `16:06.899`。
  - 结论：短切收益来自“小而保守的局部顺序短切”，不是更大搜索或更长追踪；这些放宽路线都不保留。
- 前期追踪阈值 `1/3 -> 3/8`
  - 2026-06-09 请求 `714` 验证。
  - 改法：不引入新 BFS 或函数层，只把当前 `dinosaur2` 主循环中前期追苹果阶段从 `length < size * size / 3` 延长到 `length < size * size * 3 / 8`，约多追 `43` 个尾长后再切回固定安全路径。
  - 结果：两轮 `15:29.765` / `15:32.539`，稳定均值 `15:31.152`，慢于当前历史复跑 `15:18.164`，也没有明显压过本轮 request `713` 的波动区间。
  - 结论：轻度延长前期追踪没有形成净收益；额外追苹果阶段仍会增加后续恢复成本。代码已恢复 `size * size / 3` 阈值并重新同步正式版到 `gamesave/`。后续不要继续按 `3/8` 或更长阈值上探，除非同时改变安全恢复结构。
- 前期追踪阈值 `1/3 -> 5/16`
  - 2026-06-09 请求 `727` 验证。
  - 改法：不引入新 BFS 或函数层，只把前期追苹果阶段从 `length < size * size / 3` 提前切回到 `length < size * size * 5 / 16`，约在尾长 `320` 左右转入固定安全路径，测试“更早恢复安全形状”是否优于默认。
  - 结果：两轮 `15:40.156` / `15:47.899`，稳定均值 `15:44.027`，明显慢于当前历史复跑 `15:18.164`。
  - 结论：更早切回固定路径也没有净收益；它减少了前期追苹果复利，但没有换回足够恢复收益。代码已恢复 `size * size / 3` 阈值并重新同步正式版到 `gamesave/`。后续不要继续在 `5/16..3/8` 之间做单变量阈值微调。
- 列对收苹果 / 行蛇形 / Dinosaur 实体旁路
  - 2026-05-02 请求 `569` / `571` / `572` 验证。
  - 结果：
    - 列对收苹果候选无完成轮，`cactus` 在 `999992704` 后不再下降，取消摘要 `420:23.252` 不作为有效成绩。
    - 行蛇形替换固定回路无完成轮，`cactus` 在 `999997312` 后不再下降，取消摘要 `158:05.657` 不作为有效成绩。
    - 尝试利用 `DinosaurHat#BoneCount()` 统计网格上所有 `Dinosaur` 实体的旁路无有效 `run=` 输出，被 leaderboard 取消。
  - 结论：当前 `dinosaur2` 的列蛇形安全恢复结构不能随意替换；`Dinosaur` 实体不能作为普通 `plant()` 旁路。
- 前 100 苹果 probe：短贪心前缀后恢复 `dinosaur2`
  - 2026-05-02 请求 `585` / `586` / `587` / `589` / `591` 验证。
  - 改法：只在开局用缓存的直达苹果路线，移动时只做 `can_move()` 兜底；达到阈值后立即切回原 `dinosaur2` 的按尾长让路、追苹果、回左下恢复循环。该 probe 到 `length=100` 后 `clear()`，取消摘要不作为完整榜成绩。
  - 结果：
    - 阈值 `25`：到 100 苹果 `112.73s`，明显优于安全路线 probe 的 `127-129s`。
    - 阈值 `35`：到 100 苹果 `109.57s`。
    - 阈值 `40`：到 25 / 50 / 75 / 100 苹果分别为 `28.92s` / `56.98s` / `83.84s` / `106.41s`，当前前 100 最佳。
    - 阈值 `45`：只到 `length=45` 后取消；阈值 `42` 也只到 `length=42` 后取消。
  - 结论：前期确实应尽量直达苹果，但可恢复阈值很窄；当前最好的可测点是 `GREEDY_LIMIT=40`。每苹果 BFS 或完整 body 模拟仍然太贵，前 100 阶段优先保留 O(1) 贪心 + 早切恢复。
- 完整 32x32 验证：前缀收益不能直接迁移到全图
  - 2026-05-02 请求 `593` / `594` / `595` / `596` / `597` / `598` / `599` 验证。
  - 改法：把前 100 probe 改成完整榜单流程，先测试 `GREEDY_LIMIT=40` / `25`，再测试同框架下的 `GREEDY_LIMIT=0` 基线；后半段固定路径必须使用无 `measure()` 的移动，否则会把完整一轮拖慢到 `24:25.099`。
  - 结果：
    - `GREEDY_LIMIT=40` 在完整随机局里只到 `length=33` 就取消，说明前 100 最优阈值无法稳定恢复。
    - `GREEDY_LIMIT=25` 可完成但很慢：修掉后半段 `measure()` 前为 `24:25.099`，修掉后第一轮 `18:00.999`、第二轮 `26:15.351`，后续低仪表测试还出现 `27:52.148`。
    - 同框架 `GREEDY_LIMIT=0` 基线为 `16:51.952` / `17:07.899`，均值 `16:59.926`；仍慢于历史原版 `15:24` 左右，说明重构出的统计/函数层也会污染完整榜时间。
    - 回退到稳定 `dinosaur2` 主线后，请求 `599` 两轮为 `15:25.859` / `14:57.890`，均值 `15:11.875`。
  - 结论：完整榜不能把前 100 的裸贪心前缀直接留下。后续优化必须在原 `dinosaur2` 紧凑代码结构内做局部、可恢复的短切，不能引入每步 probe、完整 body 模拟、或额外函数层。当前 `.py` 已回退到稳定 `dinosaur2` 主线。

## 下一步优化方向

- 重点确认：
  - `dinosaur2` 的切换阈值 `size * size / 3` 是否最优
  - 前期受限追踪里是否还存在可以安全缩短的路径；下一次不能再试普通 BFS，需要把可恢复性纳入状态或只在原安全路径邻域内短切
  - 后期 `simple_update_and_move(path[x][y])` 结算前是否存在可控提速
- 已验证把前期追踪阈值轻度延长到 `3/8` 没有刷新；默认继续保留 `size * size / 3`。
- 已验证把前期追踪阈值提前到 `5/16` 也没有刷新；后续不要继续做单变量阈值微调。
- 优化目标应明确写成：
  - 不是做分段 cash-out
  - 而是保留 `dinosaur2` 的“前期追苹果、后期保命”结构，继续压缩吃苹果等待步数

## 候选策略方向（猜测 / 待验证）

### 方向 1：哈密顿主回路 + 受限安全捷径

- 核心思路：默认沿固定回路走，但当苹果落在局部安全窗口内时允许短切
- 主瓶颈：纯哈密顿回路安全，但吃苹果的平均等待可能偏高
- 可能更强的原因：`dinosaur2` 已证明“受限安全捷径”方向有效，后续应在它的结构内继续细化
- 优先探针：
  - 纯回路下苹果平均等待步数
  - 2x2、3x3 局部窗口内安全短切的成功率

### 方向 2：前期贪心增尾，过阈值后切回固定回路

- 核心思路：把 `main5` 的阶段思想保留，但切换阈值更早、更保守
- 主瓶颈：早期完全固定回路可能吃苹果偏慢，后期继续贪心又容易死
- 可能更强的原因：前期用有限贪心加速增尾，后期用回路保命，可能比纯任一侧更平衡
- 优先探针：
  - 不同尾长阈值下的死亡率
  - 切回回路后的苹果获取效率

### 方向 3：利用“越早吃苹果越便宜”的特性重排早期路径

- 核心思路：不是改变最终目标，而是让前几十颗苹果尽量更早被吃到，以便更早触发 `ops -= floor(ops * 0.03)` 的连锁降耗
- 主瓶颈：纯哈密顿回路虽然安全，但前期吃苹果的等待可能偏长
- 可能更强的原因：早期每少走一步，都可能通过更早降 `ops` 在后面继续放大收益
- 优先探针：
  - 前 50 / 100 个苹果的平均获取步数
  - 更激进的早期吃苹果策略对总时间的影响

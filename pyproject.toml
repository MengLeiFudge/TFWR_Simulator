[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tfwr-oracle-workspace"
version = "0.1.0"
description = "Workspace for TFWR oracle runner mod and Python orchestrator."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "MLJ" }
]

[tool.setuptools]
py-modules = []
